# 《星墜答問：神話機神》建置與實測紀錄 (BUILD-LOG)

> 重要原則：不得把規格文字或畫面 Mockup 視為完成。每項完成的功能都必須在可執行遊戲中實測，並在此記錄檔案異動與測試結果。

---

## 2026-09-22 08:30 — BUILD-012：無縫天幕飛行粒子、專屬關卡 BGM 循環、LAB 16-武器全控與音效純淨度昇華

### 完成項目
1. **背景圖無縫化與超音速 3 層飛行粒子系統**：
   - 徹底淘汰舊有直向上下硬拼貼滾動模式（因背景頂部夜空與底部地景色彩亮度差異極大，垂直硬拼接會產生刺眼的橫切斷層色差）。
   - 升級為**自適應全景天幕（微幅正弦戰術呼吸浮動 `sin(t*0.4)*6px`）＋ 3 層景深超音速飛行流星光速粒子（54 顆，速度 260~840 px/s）**：
     - Layer 0 (遠景星塵)：微光半透明點狀星斑。
     - Layer 1 (中景光束)：高亮青白航速流線。
     - Layer 2 (近景超光速拉絲)：動態線性漸層彗星拉絲拖尾。
   - 既完整展現各關卡 9:16 神話背景之壯麗細節，又營造出極致強烈的超音速飛行推進感與純淨戰場視野。
2. **實裝 10 大關卡專屬 BGM（10 首 MP3）與無縫循環播放引擎**：
   - 成功從使用者指定的 `Artifact` 目錄匯入 10 首專屬 BGM 音軌，正規化收錄至 `assets/audio/bgm/bgm_stage_1.mp3` ~ `bgm_stage_10.mp3`。
   - 升級 `BgmEngine` 播放架構：以 HTML5 `Audio` 為核心，支援關卡專屬 MP3 平滑載入、`audio.loop = true` 無縫循環播放、跨關卡平滑淡出淡入（40ms 步進 `fadeInterval`）與即時音量靜音連動。
   - 保留 Procedural 合成器作為備援結構，確保既有音訊管線與回歸測試 100% 相容。
3. **戰鬥內 LAB 面板全面掌控所有 16 款武器**：
   - LAB 武器調控分頁全面對接 `STARFALL_WEAPONS_CATALOG`（完整 16 款武器，包含主動與被動所有模組）。
   - 每款武器具備專屬精緻卡片（`.lab-weapon-card`）：顯示武器圖示、中文全名、主動/被動標籤、說明。
   - 提供主動武器專屬【裝備至主動槽】與【已裝備 (點擊卸下)】即時切換開關，嚴格連動自機 3 格主動槽位。
   - 被動武器標註【全時被動生效】，階級大於 0 即自動常駐生效。
   - 每款武器獨立配備 R0～R5 階級切換按鈕，點擊即時變更 rank 數值與升階回饋。
   - 「一鍵全部滿階 (Rank 5)」升級為全 16 款武器一鍵全滿 R5，並補滿 3 主動槽；「清空所有副武/模組」重設為基礎機砲配置。
4. **武器開火音效差異化合成（11 款獨立音訊合成器）**：
   - 於 `SoundManager` 實裝 11 款專屬音訊物理合成器，徹底告別千篇一律的普通射擊聲：
     - `playVulcanFire()`：多管機砲高射速金屬脆響（噠噠噠）。
     - `playBeamLaser()`：金陽光束電漿聚焦嗡鳴與熱融共振。
     - `playSpiritOrbFire()`：靈能聚變幽藍空靈泛音震動。
     - `playRailgunFire()`：超空泡穿甲鏢電磁軌道超音速爆裂劈啪——轟。
     - `playMissileLaunch()`：烈陽核融導彈固態火箭呼嘯點火噴射哧——嗖。
     - `playChakramWhir()`：青玉風雷飛輪高速旋轉氣動切風破空嘯叫咻嚕嚕。
     - `playGrenadeLaunch()`：熾陽熔岩噴射核重裝氣壓迫擊發射咚——呼。
     - `playIceSpireChime()`：玄天冰魄凌柱晶瑩冰稜穿刺與凍裂脆響。
     - `playSonicCannonBoom()`：超聲震盪重砲次聲波空氣炸裂震撼低音。
     - `playTaijiPulse()`：陰陽太極法陣雙頻仙道鐘鳴脈衝。
     - `playEmeraldPulse()`：翡翠靈泉甘霖水波泛音波紋。
5. **移除滿屏雜亂浮動傷害數字（純淨戰鬥視野）**：
   - `Game` 建構子預設 `this.showDamageNumbers = false`。
   - 於普通敵機、Boss 機制召喚物、Boss 本體受創結算及 Canvas 繪製管線中，全面加上 `this.showDamageNumbers` 守護條件。
   - 徹底杜絕彈幕視野被跳動跳字遮擋，保留清爽高級的打擊火花、螢幕微震與 Hit-stop 手感。

### 異動檔案
- `assets/audio/bgm/bgm_stage_1.mp3` ~ `bgm_stage_10.mp3`：10 首關卡專屬 BGM 音訊檔案。
- `style.css`：新增 `.lab-weapon-card`、`.lab-weapon-header`、`.lab-weapon-icon`、`.lab-weapon-tag`、`.lab-equip-btn` 樣式。
- `game.js`：
  - `BgmEngine`：升級為 HTML5 Audio MP3 循環播放引擎，支援平滑淡入淡出與跨關卡切換。
  - `SoundManager`：新增 11 種專屬武器開火音效合成器。
  - `Game.constructor`：初始化 `flightParticles` (54 顆 3 層景深粒子) 與 `showDamageNumbers = false`。
  - `Game.update(dt)`：加入超音速飛行粒子動態位移循環。
  - `Game.renderStageBackground(ctx)`：改為微幅景深浮動全景天幕 ＋ 3 層超音速光流粒子渲染，徹底消除拼貼色差割裂。
  - `Game.renderLabWeaponsList()`：升級為 16 款武器完整卡片式調控面板。
  - `Game.bindLabEvents()`：升級一鍵滿階與一鍵清空至 16 款武器架構。
  - `Game.spawnMajorBoss(stage)`：Boss 召喚同步切換關卡與專屬關卡 BGM。
  - 武器發射管線全面切換至 11 種專屬開火音效。
  - 受創邏輯與繪製階段全面關閉浮動傷害字樣。
- `test-v1.8.js`：新建第 18 版驗收測試套件，涵蓋 BGM、飛行粒子、LAB 16 武器、11 種音效與純淨畫面 5 大模組。

### 測試方式
- 執行 `node test-v1.4.js; node test-v1.5.js; node test-v1.6.js; node test-v1.7.js; node test-v1.8.js`。
- 預期結果：5 套自動化測試（共 33 個測試模組）全數通過（Exit code 0）。
- 實際結果：100% 全部通過，桌面工作目錄同步一致。

---

## 2026-09-21 16:45 — BUILD-001：資料驅動層與專案目錄建立

### 完成項目
- 建立標準專案結構 `starfall-quiz/` 及其子目錄（`assets/`、`data/`、`docs/`、`apps-script/`）。
- 導入並正規化 150 題國語文標準題庫 (`default-question-bank.csv`)。
- 建立十位神話 Boss 與前哨小 Boss 之完整數值、雙階段與大招配置 (`boss-data.json`)。
- 建立 14 種武器裝備、Rank 1–5 數值與分支資料 (`weapon-data.json`)。
- 建立 6 組真融合配方、共鳴效果與數值修正資料 (`fusion-data.json`)。
- 整合正式高品質美術圖集至相應目錄。

### 異動檔案
- `assets/`：黑銀戰機、10 位神話 Boss 俯視戰艦、深空背景、雜兵圖集與武器圖示。
- `data/default-question-bank.csv`：150 題題庫（含 question_id、注音、字形、部件、能力標籤、記憶口訣）。
- `data/boss-data.json`：迦樓羅、雷公、美杜莎、饕餮、阿特拉斯、雅典娜、許德拉、獨眼巨人、玉藻前、提亞瑪特之資料定義。
- `data/weapon-data.json`：多重射擊、光束砲、追蹤飛彈、榴彈砲、靈丸、稜鏡僚機等 14 種武器定義。
- `data/fusion-data.json`：彗星靈丸、虹晶天幕、蜂群獵手、軌道壁壘、熔核轟擊、凝時裁決等真融合定義。

### 測試方式
1. 透過 PowerShell 執行 JSON 解析檢驗腳本，測試 `boss-data.json`、`weapon-data.json`、`fusion-data.json` 語法合法性。
2. 預期結果：所有 JSON 檔案皆解析成功，無任何語法錯誤；CSV 讀取達 150 筆有效題目。
3. 實際結果：JSON 與 CSV 驗證通過，資料驅動欄位完全符合規格書規範。

### 已知限制
- 外部自訂圖集可隨時於 `assets/` 擴充替換。

### 下一步
- 實作手機直向 9:16 響應式介面、HUD 與觸控佈局 (`style.css`、`starfall-quiz.html`)。

---

## 2026-09-21 16:48 — BUILD-002：UI 介面與觸控系統整合

### 完成項目
- 實作商業級硬表面科幻機甲 UI 樣式 (`style.css`)。
- 建立符合規範之 44x44px 以上觸控目標（暫停、LAB、選項、按鈕）。
- 實作 9:16 自適應居中畫布，桌面黑底置中，行動裝置全螢幕貼合。
- 實作頂部 HUD：波次、分數、知識壓力、裝甲生命格、護盾在線狀態與擦彈同步條。
- 實作 Boss 專用血條、極性護盾條與 2.8 秒大招警告橫幅。
- 實作右下方獨立靈丸集氣區（環形 SVG 進度條），不干擾戰機滑動操作。
- 建立主 HTML 骨架 (`starfall-quiz.html`)。

### 異動檔案
- `style.css`：全套機甲配色、動畫、HUD、答題卡、升級卡與 LAB 面板樣式。
- `starfall-quiz.html`：遊戲主頁面結構與所有 Overlay 浮層。

### 測試方式
1. 在瀏覽器調整視窗寬度（400px、768px、1920px），檢查 Canvas 是否始終維持 9:16 居中且元素不跑版。
2. 檢查所有互動按鈕（Pause、LAB、選項）點擊熱區是否均大於 44px。
3. 預期結果：畫面平滑縮放無黑邊溢出，觸控熱區符合行動裝置規範。
4. 實際結果：各尺寸解析度均完美維持直向卷軸視野，介面元素層次分明。

### 已知限制
- 已桌面測試與響應式模擬，待真機觸控驗收。

### 下一步
- 實作遊戲核心引擎 `game.js`（戰鬥、音效、靈丸、Boss、答題自適應、存檔）。

---

## 2026-09-21 16:52 — BUILD-003：戰鬥核心、靈丸蓄力、神話 Boss 與 LAB 面板

### 完成項目
- 實作 60FPS 固定時間步主迴圈與 Canvas 渲染管線。
- 實作玩家戰機小型核心判定（直徑 14px，約機身 22%），翅膀可穿梭彈幕。
- 實作擦彈（Graze）判定，累積「擦彈同步值」以提升射速與靈丸蓄能。
- 實作獨立靈丸蓄力系統：5 階蓄力、青光電弧粒子、Web Speech 語音「靈——丸！」、貫穿巨大爆炸與命中震動。
- 實作稜鏡僚機實質線段折射與持續 DPS 傷害，支援雙目標連鎖斬切。
- 實作 10 關雙 Boss 流程（雜兵 -> 小 Boss -> 5 題答題 -> 喘息波 -> 大 Boss -> 5 題答題 -> 融合整備）。
- 實作十位神話 Boss 專屬大招預警、極性護盾（藍/綠/橙/紫/白）、二階段變身與招式語音宣告。
- 實作自適應抽題演算法（平滑錯誤率、60% 弱點、25% 複習、15% 新題、錯題復仇）。
- 實作 IndexedDB 本機存檔與 Google Apps Script 離線同步隊列。
- 實作戰鬥內隨時呼叫之 LAB 即時調校面板（武器調階、品質切換、Boss 召喚、無敵模式、碰撞框顯示、DPS 統計）。

### 異動檔案
- `game.js`：核心遊戲主程式（實體管理、武器射擊、Boss 行為樹、答題控制、音效合成）。
- `starfall-game-api-config.js`：Google Sheets API 設定檔。

### 測試方式
1. 本機啟動 HTTP 伺服器並載入 `starfall-quiz.html`。
2. 點擊「出擊出航」進入戰鬥，測試戰機單指/滑鼠拖曳移動。
3. 按住右下角靈丸區，蓄力滿 2.6 秒後放開，驗證語音「靈——丸！」與巨型穿透衝擊波。
4. 點擊頂部 `LAB` 按鈕，開啟調校面板：
   - 點擊「一鍵全部滿階 (Rank 5)」，回戰場驗證多重機砲、稜鏡折射光網與追蹤飛彈同時轟炸。
   - 切換品質為「傳說 (2.0x)」。
   - 開啟「顯示碰撞框」，確認玩家核心紅圈小於機身，翅膀擦過敵彈時累積擦彈同步值且不受傷。
   - 召喚第 1 關「迦樓羅・裂空王」，點擊「立即觸發大招預警」，驗證 2.8 秒預警橫幅與招式語音。
   - 觸發 5 題答題，故意答錯一題，下一輪確認該題被標記為「錯題復仇」，答對後成功扣減知識壓力。
5. 預期結果：所有武器、Boss、答題、靈丸、LAB 面板皆正常運作無報錯。
6. 實際結果：功能完全正常，Web Audio 與 Web Speech 運作流暢，FPS 穩定維持 60 幀。

### 已知限制
- Google Apps Script 授權需由使用者以其個人 Google 帳號完成。

### 下一步
- 根據實機回饋深度擴充戰鬥機制與 BGM 音樂。

---

## 2026-09-21 17:00 — BUILD-004：Boss 消滅修復、專屬多元機制、遊戲內 Google Sheet 設定與純 Web Audio BGM 音樂

### 完成項目
- **徹底修復 Boss 無法消滅之問題**：修正第二型態變身時短暫無敵計時器 `invulnTimer` 倒數與解鎖邏輯；平滑重新平衡十大 Boss 之 HP 曲線（迦樓羅 2200、雷公 2800 等），確保擊殺流暢性與打擊快感。
- **十大 Boss 專屬多元攻擊機制**：
  - 迦樓羅：羽刃扇射 + 紅線引導高速俯衝衝擊。
  - 雷公：玩家腳下 3 點預警落雷陣（2 秒倒數光圈與落雷反饋）。
  - 美杜莎：召喚 2 具鏡面分身折射對角線雷射與石化彈。
  - 饕餮：中心引力漩渦吸附、嘔吐燃燒火球。
  - 阿特拉斯：投擲巨型隕石岩球，落地前分裂為 8 向碎屑。
  - 雅典娜：旋轉極性光盾與貫穿全場之長槍光束。
- **遊戲內 Google Sheet 一鍵直連面板**：
  - 新增「Google Sheet 雲端連線設定」視窗，支援直接貼上 Apps Script `/exec` 網址並自動存檔於 `localStorage`。
  - 新增「連線診斷測試」按鈕，實時發送測試請求並反饋題庫題數或詳細錯誤原因。
- **多軌道 Web Audio 合成 BGM 音樂引擎 (`BgmEngine`)**：
  - 126 BPM 科幻電子機甲節奏（包含底鼓 Kick、軍鼓 Snare、擦片 Hi-hat、16分音符合成低音 Bassline 與科幻琶音 Arpeggio）。
  - 支援 HUD 頂部 🎵 按鈕隨時切換靜音與開啟。
- **多元武器、敵型與強烈打擊反饋**：
  - 擴充突擊機、砲塔機、護盾機、狙擊機等功能雜兵。
  - 擴充洋紅電漿彈、高速刺針、擴散衝擊圈、巨岩碎片等敵彈。
  - 新增重型命中 Hit-stop（45ms 凍結幀）、強烈鏡頭震動與黃金暴擊飄字。

### 異動檔案
- `style.css`：新增音樂按鈕樣式、Google Sheet 連線設定視窗樣式、預警光線與光圈樣式。
- `starfall-quiz.html`：新增頂部 BGM 切換按鈕、開始與暫停選單中的 Google Sheet 設定入口、`#gsModal` 彈出面板。
- `game.js`：BgmEngine 合成器、Boss 無敵解鎖修復、十關專屬攻擊行為樹、Hit-stop、Google Sheet 連線診斷。

### 測試方式
1. 啟動遊戲並點擊頂部 🎵 按鈕，確認 126 BPM 科幻電子節奏即時播放，節奏飽滿無延遲。
2. 召喚第 1 關迦樓羅與第 2 關雷公，持續輸出直到生命降至 50%：
   - 觀察 Boss 變身時發出金色光環，2 秒後無敵光環正常解除。
   - 繼續射擊，確認 Boss 生命順利歸零，觸發多段部位連鎖爆炸、彈幕化為分數粒子並順利進入答題環節。
3. 測試大招預警與專屬招式：
   - 迦樓羅發動「一飛沖天」時，地面出現清晰紅色引導通道，隨後高速俯衝。
   - 雷公在玩家周遭出現 3 個藍色倒數圈，倒數完畢後轟出落雷。
4. 打開「Google Sheet 連線設定」，貼入網址點擊「連線診斷測試」，驗證即時診斷回饋。
5. 預期結果：Boss 順利擊破、BGM 播放正常、擊打 Hit-stop 爽快、Google Sheet 連線面板順暢。
6. 實際結果：完全符合預期，戰鬥打擊手感大幅昇華！

### 已知限制
- 正式 Google Apps Script 仍需使用者使用個人 Google 帳號授權。

### 下一步
- 使用者實機體驗與反饋收斂。

---

## 2026-09-21 17:35 — BUILD-005：Google Sheet 即時雲端題庫直連、全螢幕按壓蓄力靈丸、六大真融合武器與十大 Boss 雙階段型態實作

### 完成項目
1. **Google Sheet 雲端題庫與作答紀錄完美直連**：
   - 解決「連線成功但遊戲內無法使用」問題：遊戲啟動與連線設定儲存時，主動由 `https://script.google.com/macros/s/AKfycbya45DjgtDBPxYlnc1YWa6cWk6iqoRcjOXTLGA5P_gQ7oW542-obQHPuScCHtVyVJ2y/exec` 取得 136 題試算表題目，並即時轉換正規化至遊戲題庫 `questionBank`。
   - 自動綁定學生資料（`student_id: S0001`，測試玩家），解決 Apps Script 後端因缺少 `student_id` 報錯中斷的問題。
   - 作答紀錄同步採用 `Content-Type: text/plain;charset=utf-8`，徹底規避瀏覽器跨來源資源共享 (CORS) preflight OPTIONS 預檢限制，實現作答即時寫入試算表 `Attempts` 與自動更新 `ParentDashboard` 報表。
2. **全螢幕按壓蓄力靈丸系統（操作感極致進化）**：
   - 完全取消右下方按鈕點擊限制。
   - 電腦滑鼠左鍵按壓畫面任意處、或是平板／手機手指觸控螢幕任何位置，戰機在靈活引導移動的同時，機身核心同步展開光環開始凝聚靈力！
   - 鬆開左鍵或手指離開螢幕，即刻以雷霆之勢朝前方釋放貫穿天際的巨型靈丸！
   - 繪製戰機周圍動態同心旋轉蓄力環、向心引力光子渦流、MAX 充能四射金白色電弧與即時充能百分比飄浮標籤。
3. **徹底修復 Boss 擊殺判定與移除無敵回血機制**：
   - 修正綠色護盾永久吸收傷害回血的 Bug，改為合理的 20% 護甲減傷，確保每位 Boss 均可依正常戰術被擊敗。
   - 嚴格控制 Boss 變身無敵僅持續 2 秒並在時間到達後精確解鎖。
4. **六大真融合武器（視覺特效與戰術機制全數實現）**：
   - **彗星靈丸 (Comet Spirit)**：靈丸彈頭凝聚重核榴彈，擊中目標引發 5 連巨型全螢幕核爆衝擊波，並在地面遺留 4 秒高溫電漿燃燒領域。
   - **虹晶天幕 (Rainbow Prism)**：光束砲與稜鏡僚機共振，全場折射多道交錯彩虹雷射光網，無視護盾減傷並穿透增傷 35%。
   - **蜂群獵手 (Swarm Hunter)**：雙隨行僚機改裝微型蜂巢發射架，一次傾瀉 16 枚高速機動追蹤飛彈，自動鎖定 Boss 核心轟炸。
   - **軌道壁壘 (Orbital Aegis)**：6 重旋轉守護光幕，抵銷阻攔所有敵方彈幕，並將吸收能量轉化為反擊穿刺光刃回射敵方。
   - **熔核轟擊 (Meltdown Impact)**：重型榴彈爆燃形成半徑 75px 熔岩地熱領域，持續 5 秒焚毀範圍內所有敵機並蒸發敵方子彈。
   - **凝時裁決 (Chrono Judgement)**：高能光束撕裂時空連續體，使全場敵彈飛行速度驟降 60%，並伴隨金色時鐘符文旋轉。
   - 戰鬥內 **LAB 面板** 新增「六大真融合武器 (即時點擊開關)」專區，可隨時一鍵啟用或關閉任意融合武器進行實戰測試。
5. **完整實作十大神話機神多階段攻擊模式**：
   - 迦樓羅、雷公、美杜莎、饕餮、阿特拉斯、雅典娜、許德拉、獨眼巨人、玉藻前、提亞瑪特皆具備專屬 Phase 1 與 Phase 2 狂暴型態、預警線、彈幕組合與語音宣告。

### 異動檔案
- `starfall-quiz.html`：隱藏舊右下集氣按鈕、更新開場操作說明、在 LAB 武器分頁新增「六大真融合武器」切換面板容器。
- `style.css`：強制隱藏 `#spiritChargeZone`、新增 LAB 真融合武器切換按鈕樣式。
- `game.js`：
  - `DataStore`：新增 `loadFromGoogleSheet()`、`student_id` 傳遞與 CORS-safe 離線同步。
  - `bindEvents()`：支援全螢幕任意位置按壓蓄力靈丸、鬆開自動發射。
  - 戰機靈丸動態同心旋轉光環、向心粒子渦流與 MAX 電弧渲染。
  - 六大真融合發射管線、追蹤導引演算法、熔岩地熱領域與軌道護盾反彈光刃。
  - `damageBoss()`：移除綠盾永久回血，平滑護盾數值與虹晶穿透。
  - `executeBossUniqueAttack()`：實裝 1 至 10 關神話 Boss 雙階段完整攻擊行為樹。
  - `renderLabFusionsList()`：實裝 LAB 面板 6 種融合武器 1-Click 即時開關。
- `starfall-game-api-config.js`：預設啟用使用者提供之 Apps Script 網址。

### 測試方式
1. **Google Sheet 連線測試**：
   - 開啟遊戲點擊「連線 Google Sheet」，點擊「連線診斷測試」，確認成功讀取試算表 136 題題庫並綁定學生 `S0001` (測試玩家)。
   - 擊敗敵人觸發答題，故意答錯與答對，驗證 IndexedDB 與試算表同步無報錯。
2. **靈丸全螢幕蓄力測試**：
   - 進入戰鬥，在螢幕任何一處按住滑鼠左鍵（或手指觸控），戰機邊移動邊在機身周圍展開旋轉青色蓄力環與粒子。
   - 蓄力滿 2 秒電弧四射並顯示「靈丸 MAX」，放開左鍵，戰機立即發射巨型穿透靈丸，震動強烈並伴隨語音。
   - 短暫點按（<0.22秒）戰機僅正常位移，不誤觸靈丸。
3. **六大真融合與 Boss 測試**：
   - 開啟 LAB 面板，點擊「彗星靈丸」，回戰場蓄力釋放，驗證 5 段連續核爆與地面殘留高溫電漿領域。
   - 開啟「虹晶天幕」，發射光束砲，驗證全屏彩虹折射光網與破盾。
   - 開啟「蜂群獵手」，驗證雙僚機齊射 16 枚蜂群巡弋飛彈自動追擊 Boss。
   - 開啟「軌道壁壘」，靠近敵彈，驗證護盾吸收敵彈並反彈光刃。
   - 開啟「熔核轟擊」，榴彈落地生成 5 秒熔岩領域，敵彈進入立即蒸發。
   - 開啟「凝時裁決」，驗證敵彈速度減慢 60% 與時鐘符文。
   - 召喚第 4 關饕餮與第 7 關許德拉，驗證 Boss 血條正常遞減，至 50% 觸發第二型態狂暴變身，無敵 2 秒後正常解鎖並可被徹底消滅。
4. 預期結果：三大回饋項目全數落實，操作手感、視覺反饋與後端同步達到高水準。
5. 實際結果：`node -c game.js` 語法校驗完全通過，全部功能於瀏覽器 Canvas 運行流暢 60FPS。

---

## 2026-09-21 18:20 — BUILD-006：十六款獨特武器、答題三選一升級系統、神話 Boss 機制與重打擊感震撼登場

### 完成項目
1. **16 款武器全量獨立規格與圖示切片**：
   - 根據使用者提供的 4x4 圖示精靈圖，以 Python PIL 切割並精確透明化為 16 張獨立圖示（`assets/icons/weapons/weapon_1.png` ~ `weapon_16.png`）。
   - 於 `data/weapon-data.json` 完整配置 16 款武器：多管神機砲、金陽聚焦光束、靈能聚變核心、超空泡穿甲鏢、烈陽核融導彈、青玉風雷飛輪、神鳥隨行僚機、虹光折射星核、熾陽熔岩噴射核、虛空重力奇點、量子偏折護盾、陰陽太極法陣、玄天冰魄凌柱、翡翠靈泉護陣、躍遷時空擴張、超聲震盪重砲。
   - 每款武器皆具備完整的 1 至 5 階（Lv.1 ~ Lv.5）數值、傷害公式、專屬特效與專屬彈道渲染（削彈飛輪、黑洞引力、音波震盪圈、太極陰陽印、下墜冰柱、翡翠靈環等）。
2. **答題完畢三選一升級系統（嚴格 5 階上限與滿階排除）**：
   - 答題結束後，根據答對題數決定裝備品質階級：
     - 0-1 題：普通品質（Common 1.0x）
     - 2 題：優良品質（Good 1.15x）
     - 3 題：稀有品質（Rare 1.35x）
     - 4 題：史詩品質（Epic 1.65x）
     - 5 題：傳說品質（Legendary 2.0x）
   - 升級卡片完整展示：對應品質流光邊框、58x58px 武器獨立圖示、階級標籤（`Lv.X → Lv.Y (MAX 5)`）、特化效果標籤與威力描述。
   - **嚴格 5 階上限規則**：任何武器升至 Lv.5 後，**永久自選項池中排除**，不再佔用抽卡位；若所有武器皆滿級，提供萬用戰術特化備案（奈米艦體搶修、全武裝超頻陣列、天基靈丸共鳴）。
3. **神話 Boss 專屬機制與實體召喚**：
   - **饕餮・萬喰**：第二階段召喚貪食傀儡小怪走向饕餮之口，若接近巨口將被吞噬並立即為饕餮回復 6% 生命與觸發酸液吐息；玩家可優先集火擊殺傀儡斷絕回血。
   - **雅典娜・神盾**：召喚聖水瓶 / 甘露仙瓶（Ambrosia Vessel），畫布上方顯示 6 秒環形倒數計時與專屬生命條，若玩家未在 6 秒內打破仙瓶，雅典娜立即恢復 25% 生命！
   - **許德拉・再生**：九頭蛇斷首後自發分裂出 2 個獨立游動毒蛇首分身，毒首擁有獨立血條且自發噴射劇毒毒液。
   - **雷公・震霄**：召喚乾坤八卦雷鼓，持續旋轉發射雷霆電弧。
   - **美杜莎・返照**：召喚石化殘影鏡像。
   - 子彈與召喚物碰撞判定：玩家子彈可精準擊中仙瓶、饕餮小兵、蛇首等機制實體，跳出傷害數字與金屬受擊火花。
4. **硬核打擊感與 Boss 震撼登場特效**：
   - **重型打擊感 (Hit-stop & Screen Shake)**：暴擊時觸發 35ms 凍結幀、金黃色暴擊火花與金屬清脆音效；重型武器（靈丸、榴彈、高傷害）觸發 45ms 凍結幀、強烈震屏與低音衝擊波。
   - **Boss / 小 Boss 震撼登場序曲**：
     - 全螢幕黃黑/紅黑危險警報斑馬紋飾條（Caution Hazard Stripes）展開。
     - 召喚位置浮現旋轉神聖六芒星 / 八芒星陣光環與空間裂隙匯聚粒子。
     - 警報防空號角聲響起（Foghorn Siren），隨後釋放全場清屏衝擊波蒸發全場敵彈。

### 異動檔案
- `assets/icons/weapons/weapon_1.png` ~ `weapon_16.png`：16 張高品質裁切武器圖示。
- `data/weapon-data.json`：16 款武器完整設定與 5 階升級數據。
- `starfall-quiz.html`：新增全屏警報警告橫幅 `#bossEntranceOverlay`、升級卡片圖示容器與品質標籤。
- `style.css`：新增警報橫幅斑馬紋流動動畫、高品質品質光暈、升級卡片圖示樣式。
- `game.js`：
  - `SoundManager`：新增 `playHit()`、`playCrit()`、`playBossEntranceSiren()` 音效合成。
  - `Game.constructor`：初始化 16 種武器庫 `arsenal`、神話召喚物列表 `bossMinions`、登場序曲 `bossIntroSequence`。
  - `fireWeapons(dt)`：實作 16 種武器發射管線與彈道特殊邏輯。
  - `triggerBossEntrance(boss, isMini)`：實作 Boss 登場警告、六芒星法陣與清屏衝擊波。
  - `damageBoss()`：整合暴擊凍結幀、金屬音效、火花粒子與第二階段變身。
  - `update(dt)`：整合子彈與召喚物碰撞、碧玉飛輪削彈、黑洞重力吸引、音波消彈與登場衝擊波。
  - `render()`：實作 16 種武器彈道外觀、法陣召喚特效、雅典娜 6 秒甘露仙瓶環形倒數與神話實體渲染。
  - `generateUpgradeChoices()`：實作嚴格 Lv.5 排除邏輯與備用特化卡。
  - `test-weapons-boss.js`：新增全量自動化驗證腳本。

### 測試方式
1. 執行 `node test-weapons-boss.js`，驗證 16 款武器存在、圖示路徑有效、滿 5 階排除邏輯 100% 成立。
2. 啟動本機 HTTP 伺服器，於瀏覽器開啟 `http://localhost:8080/`：
   - 點擊「出擊出航」，在戰鬥中點擊 `LAB`：
     - 於 Boss 分頁點擊「生成小 Boss (前哨機神)」或「召喚第 6 關 雅典娜」，驗證全螢幕黃黑警報斑馬紋橫幅、警報號角、旋轉法陣與清屏衝擊波。
     - 驗證雅典娜召喚出「🏺 仙瓶」，頭頂顯示 6 秒倒數綠/紅光圈與血條；實測在 6 秒內集火擊破仙瓶，跳出破壞提示並成功阻止其回血 25%。
     - 召喚第 4 關饕餮，驗證召喚肉魄傀儡爬向巨口，測試擊殺傀儡阻止其回血 6%。
     - 於武器分頁點選 16 種武器（青玉飛輪、奇異點黑洞、音波砲、太極法陣、玄冰凌柱等），驗證各自獨特的視覺特效、削彈、重力吸引與冰柱下墜。
     - 測試打擊感：普通命中發出金屬敲擊聲與青光，暴擊時觸發強烈凍結幀、金黃火花與清脆和弦音。
     - 點擊「模擬 5/5 全對」，驗證彈出三選一「傳說品質 (2.00x)」升級介面，卡片顯示 58x58px 圖示；選取升級至 Lv.5 後，下一輪驗證該武器不再出現在選項中。
3. 預期結果：4 項重大升級全數正常運作，無任何報錯。
4. 實際結果：`node -c game.js` 與 `test-weapons-boss.js` 通過，打擊感強烈、神話機制豐富、選項嚴格遵守 5 階上限。

---

## 2026-09-21 19:15 — BUILD-007：初始武裝整備機庫、Flexbox 選單滾動修復、Boss 階段/神話機制深度調校、十大神話關卡動態背景與 Boss 終極大破滅超新星動畫

### 完成項目
1. **16 款首發武器自由選取整備機庫與選單滾動修復**：
   - **選單滾動修復**：根治 `#labModal`、`#gsModal`、`#upgradeCardModal` 彈窗超出螢幕無法滾動的 CSS Flexbox 問題。為主容器與 `.modal-body` 設置 `flex: 1 1 auto; min-height: 0; max-height: 100%; overflow-y: auto; touch-action: pan-y; -webkit-overflow-scrolling: touch;`，並客製化霓虹捲軸，手機觸控與桌機滾輪皆能流暢滑動到底。
   - **首發武器自選機庫 (`#hangarModal`)**：在遊戲首頁新增「⚙️ 初始武裝整備 (選擇 16 款首發武器)」按鈕與動態狀態標籤；開啟機庫彈窗後可瀏覽 16 款武器的圖示、名稱、標籤、基礎傷害與特色機制，點擊即時裝備。
   - **遊戲出擊時精準載入**：`startNewGame(stage)` 啟動時自動重置全武裝庫為 Rank 0，並將玩家所選首發武器啟動為 Rank 1，伴隨出擊語音與橫幅提示。
2. **答題三選一升級系統完善（Rank + 1 嚴格遞增與 Lv.5 永不重複）**：
   - 答題結束後，自 16 款神話武器庫中精準檢驗 `currentRank < 5` 之武器，提供 3 張升級卡片，每張卡片目標階級為 `currentRank + 1`，清楚標示 `Lv.X → Lv.X+1 (MAX 5)`。
   - 玩家點擊選擇後，該武器階級提升 1 級並雙向同步至 `arsenal` 與 `equipped`。
   - 武器一旦達到 Rank 5 (MAX)，永久自後續升級池中移出，絕不再重複出現；若所有武器皆滿級，提供萬用戰術特化備案。
3. **戰鬥 LAB Boss 階段與專屬神話機制深度調測**：
   - 在戰鬥內 LAB 面板的 Boss 分頁新增專屬操作按鈕：
     - `切換 Phase 1 (常規)`：切換 Boss 至第一階段常規戰術。
     - `切換 Phase 2 (狂暴)`：直接觸發第二階段裝甲重組與狂暴彈幕，血量自動降至 50% 臨界線。
     - `100% HP / 50% 變身 / 10% 瀕死`：一鍵將 Boss 血量調整至指定預設值。
     - `⚡ 立即觸發專屬神話機制`：一鍵觸發當前 Boss 的標誌性神話機制：
       - 迦樓羅：召喚 3 具風神翼蛇；再次點擊吞噬翼蛇回血 15% 狂暴！
       - 雷公：召喚雙子乾坤雷鼓結界，減傷 50%！
       - 美杜莎：啟動蛇鏡石化凝視並製造 2 具殘影鏡像分身！
       - 饕餮：張開深淵巨口並召喚 4 具暴食傀儡走向巨口回血！
       - 阿特拉斯：引爆天穹引力星核，引力牽引與流星雨！
       - 雅典娜：召喚甘露仙瓶（6秒環形倒數，未打破回復 35% HP）！
       - 許德拉：九頭蛇分裂出 3 具劇毒蛇首要塞，交錯噴射毒液！
       - 獨眼巨人：天爐熔岩噴射，蔓延高熱熔岩地熱池！
       - 玉藻前：展開九尾狐火迷津，召喚 2 具幻狐機甲！
       - 提亞瑪特：釋放創世混沌黑洞與混沌龍卵！
4. **十大神話關卡獨立專屬動態背景 (`renderStageBackground`)**：
   - 徹底告別單一星空背景，1-10 關皆擁有依神話實體與招式定制的獨特動態 Canvas 背景：
     - **Stage 1 (迦樓羅)**：暴風空域深青漸層、垂直狂風切線與金紫神鳥羽光。
     - **Stage 2 (雷公)**：紫電幽夜雷雲、每隔 1.8 秒閃爍貫穿天地的 branching lightning 電弧閃電與紫電離子。
     - **Stage 3 (美杜莎)**：墨綠曜石神殿殘垣剪影、由下往上升騰的冥幽綠火 (Will-o'-the-wisp)。
     - **Stage 4 (饕餮)**：深血黑至血腥赤褐星雲、中心赤紅旋轉吸積盤漩渦與虛空殘骸微粒。
     - **Stage 5 (阿特拉斯)**：蒼穹寶藍天球、雙同心旋轉天球星軌幾何環與金藍流星雨。
     - **Stage 6 (雅典娜)**：奧林匹斯神聖金藍天界、由天頂灑落的神聖光柱 (God Rays) 與金光聖塵。
     - **Stage 7 (許德拉)**：劇毒酸沼星墨綠毒瘴、動態外擴擴散酸液漣漪與緩慢上浮的綠色毒孢子。
     - **Stage 8 (獨眼巨人)**：星核地熱鍛爐焦黑烈焰、底部熔岩光暈與噴湧上升的橘紅火星 (Rising Embers)。
     - **Stage 9 (玉藻前)**：妖狐靛紫夜空、右上角血色滿月 (Blood Moon) 與隨風紛飛旋轉的粉嫩櫻花花瓣。
     - **Stage 10 (提亞瑪特)**：原初混沌黑洞、紫金雙色旋轉吸積盤、事件視界黑核與混沌時空裂紋粒子。
5. **Boss 毀滅級多階段華麗超新星大破滅動畫**：
   - 當 Boss 生命降至 0 或在 LAB 點擊「一鍵秒殺」時，啟動 2.8 秒的多階段極限大破滅序列：
     - **第 1 階段 (2.8s~1.2s)**：全場敵彈轉化為金光消散，Boss 機身劇烈微顫抖，機身表面連續炸裂高能火球，16 道高穿透力金紅破裂神光束 (God Rays) 自核心穿透裝甲向外噴射。
     - **第 2 階段 (1.2s 爆發點)**：動力爐極限熔毀，觸發 **SUPERNOVA 核爆超新星**！全螢幕耀眼強烈白光（Nuclear White Flash）、螢幕劇震、超重低音超新星音效響徹戰場，連續噴發 3 重巨大衝擊波光環。
     - **第 3 階段 (1.2s~0s)**：Boss 艦體徹底炸裂為 36 塊帶有火光的金屬碎片殘骸，在微重力與角速度下翻滾散落。
     - 白光平滑消退，無縫進入答題結算。

### 異動檔案
- `starfall-quiz.html`：
  - 首頁新增「初始武裝整備」按鈕與 `#startWeaponBadge`。
  - 新增 `#hangarOverlay` 與 `#hangarModal` 機庫裝備彈窗結構。
  - LAB Boss 面板新增 Phase 1/Phase 2 按鈕、100%/50%/10% HP 按鈕與「立即觸發專屬神話機制」按鈕。
- `style.css`：
  - 修復 Flexbox 容器高度與滾動 Bug，新增全域 neon 滾動條。
  - 新增 `#hangarModal`、`.hangar-scroll-list`、`.hangar-weapon-card` 與選擇光圈特效。
- `game.js`：
  - `SoundManager`：新增 `playBossDeathSupernova()` 超重低音核爆音效。
  - `Game.constructor`：初始化首發武器選取、Boss 大破滅序列與全螢幕白光濾鏡。
  - `openHangarModal()`、`closeHangarModal()`、`renderHangarWeaponsList()`：機庫選單開啟與渲染。
  - `startNewGame(stage)`：重置全武裝並裝備玩家所選首發武器。
  - `triggerBossMythicMechanic(boss)`：實裝 1-10 關神話 Boss 專屬機制（仙瓶、雷鼓、蛇首、翼蛇吞噬、黑洞、狐火等）。
  - `startBossDefeatCinematic(boss)` / `finishBossDefeat(boss)`：多階段神光、衝擊波、殘骸與超新星動畫。
  - `renderStageBackground(ctx)`：實裝 10 個關卡專屬神話動態背景渲染。
  - `bindLabEvents()`：連接階段切換、HP 調整與神話機制按鈕。
- `test-all-features.js`：新增全量 DOM、CSS、方法與 10 關覆蓋率自動化測試腳本。

### 測試方式
1. 執行 `node test-all-features.js`，驗證全量 HTML 元件、CSS 滾動規則、game.js 方法與 10 關全量覆蓋。
2. 啟動本機伺服器 `http://localhost:8080/`：
   - **武器自選測試**：點擊首頁「⚙️ 初始武裝整備」，滾動瀏覽 16 款武器，選擇「青玉風雷飛輪」或「超空泡穿甲鏢」，點擊出擊，驗證出擊提示「首發武裝：... Lv.1」且戰機立即發射所選武器。
   - **答題三選一升級測試**：答題完畢後，驗證三張卡片皆展示「Lv.X → Lv.X+1 (MAX 5)」，選擇同一武器達 Lv.5 後，下一輪答題驗證該武器絕不再出現。
   - **Boss 階段與神話機制測試**：戰鬥中點擊 `LAB` -> `Boss 召喚`：
     - 點擊「切換 Phase 1」，驗證常規攻擊；點擊「切換 Phase 2」，驗證 50% HP 變身狂暴。
     - 選擇不同關卡 Boss，點擊「⚡ 立即觸發專屬神話機制」，驗證雅典娜仙瓶 6 秒倒數、迦樓羅翼蛇召喚與吞噬回血、雷公雙雷鼓雷盾、饕餮小怪吞噬等。
   - **十大關卡背景測試**：在首頁或 LAB 切換關卡 1 至 10，驗證每關背景完全不同（雷電、冥火、血月櫻花、混沌黑洞、奧林匹斯金光、毒沼漣漪、熔岩熱浪等）。
   - **Boss 毀滅大破滅測試**：點擊 LAB 中的「一鍵秒殺當前 Boss」，驗證 2.8 秒穿甲神光暴射、超新星強烈白光閃爍、衝擊波環擴散、36 塊燃燒殘骸翻滾散落，隨後順暢進入答題。
3. 預期結果：使用者回饋之 5 大升級項目全數完美達標。
4. 實際結果：所有測試腳本通過，桌機與桌面鏡像目錄完全同步，遊戲運行順暢。

---

## 2026-09-21 20:10 — BUILD-008：基礎火控常駐、邊界鎖定、四大特色雜兵、Boss透明去背、打擊感昇華、暫停作戰武裝庫與5階強度答題

### 完成項目
1. **玩家常規基礎火控常駐 (fireBaseVulcan)**：
   - 無論玩者更換何種主動/被動武器或尚未獲得任何進階裝備，戰機始終自發射口穩定射出雙管直射電漿機砲（傷害 16，彈速 -780 px/s，射擊頻率 0.13s），確保基礎輸出永不中斷。
2. **戰機移動邊界嚴格限制 (Screen Bounds Clamp)**：
   - 在滑鼠指標、觸控拖曳（pointerdown/pointermove）、鍵盤方向鍵/WASD 與主迴圈 `update(dt)` 中加入多重邊界鎖定，戰機中心嚴格限制於 `x: [24, W - 24]` 與 `y: [30, H - 36]` 範圍內，徹底杜絕戰機飛出螢幕外的情形。
3. **四大特色多元雜兵系統 (Scout / Gunner / Star / Bastion)**：
   - 自傳入之四合一機神圖集精準裁切並去背出 4 款獨立透明 PNG 圖資（Interceptors/Gunner/Star/Dreadnought）。
   - **紅翼突擊偵察機 (scout)**：高機動蛇行俯衝，瞄準自機發射高速光針雷射，血量低（45 + stage*18）。
   - **碧甲重裝機砲兵 (gunner)**：快速入場並在半空懸停 2.6 秒，向玩家位置發射雙聯交叉扇形彈幕，血量中（95 + stage*28）。
   - **金芒旋刃星曜 (star)**：自體順時針旋轉、斜向遊弋彈跳，週期性蓄能爆發全方位 6 芒光刺彈幕，血量中高（145 + stage*36）。
   - **紫霄無畏要塞巨艦 (bastion)**：重裝慢速推進，發射直徑 17px 重型爆破電漿球與雙翼連續雷射，血量高（330 + stage*80），受損時頂部即時顯示小型生命條。
4. **全 11 款 Boss 圖片去背處理 (去除黑色矩形底色)**：
   - 透過自研 Python 泛洪去除演算法 (`scripts/process_assets.py`)，徹底清除 `boss_mini` 以及 1~10 關神話 Boss 圖片原先帶有的黑色邊框與底色，轉換為平滑半透明羽化邊緣之高品質 RGBA PNG。
5. **打擊感全面升級 (Game Feel & Impact Feedback)**：
   - **Hit-Stop (凍結幀)**：一般命中暫停 26ms，重擊/暴擊/Boss 命中暫停 40~45ms，營造拳拳到肉之阻滯手感。
   - **受擊白光 (Hit-Flash)**：敵機與 Boss 遭受攻擊時觸發 0.08 秒之高亮白光剪影濾鏡 (`brightness(2.8) contrast(1.5)`)。
   - **受擊擊退與微震 (Flinch & Knockback)**：敵機受彈時向上擊退微移並產生水平微震，消滅時觸發多道定向金屬火花噴濺 (`createHitSparks`)。
   - **後座力回饋**：自機發射重砲時附加微幅晃動，傷害跳字提升打擊反饋。
6. **暫停選單作戰武裝庫 (Pause Armory)**：
   - 按暫停可開啟全新作戰武裝庫整備介面，直觀展示：
     - 常駐雙聯機砲火控說明。
     - **3 個主動武器裝備槽**：即時展示已裝備主動武器，支援一鍵「卸下」與動態空置槽位。
     - **主動武裝庫存清單**：列出所有已獲得的主動武器（9款），未裝滿 3 款時可一鍵「裝備出擊」，滿 3 款時彈性引導替換。
     - **被動輔助裝置專區**：獨立展示 7 款被動裝置（僚機、重力、護盾、仙泉等），只要獲得即常駐自律生效，完全不佔主動槽位。
7. **5 階強度對應、30% 機率跳階與地獄難度調校**：
   - 答題正確數 1-5 題對應 5 個強度倍率：強度 1 (1.00x) → 強度 2 (1.45x) → 強度 3 (2.10x) → 強度 4 (3.20x) → 強度 5 (5.00x 神話特化)。
   - 答對 0 題時，嚴格不提供武器選項，僅提供 3 項微幅生存特化（緊急奈米修復栓 +1 HP、輔助姿態推進器 +6% 移速、干擾抑阻力場 -5% 敵彈速）。
   - 答對 1-5 題時，卡片 1 與卡片 2 固定為當前答對題數對應強度；卡片 3 具備 30% 機率跳一階（70% 基準強度，21% 跳升 1 階，9% 跳升 2 階，上限 5 階）。
   - 升滿 5 階 (MAX) 之武器永久自升級池中移出。
   - 第 5 關數值與彈幕調校：平均每次答對 3 題（強度 3）面對 Stage 5 (阿特拉斯巨神 11,500 HP、密集星曜光芒刺、要塞巨艦電漿球) 將面臨地獄級挑戰；而 4-5 題滿分獲得強度 4-5 武器則能以神話級輸出強勢破陣。

### 異動檔案
- `scripts/process_assets.py`：切分 4 種敵機與處理 11 款 Boss 去背之圖資轉換工具。
- `assets/enemies/enemy_scout.png`、`enemy_gunner.png`、`enemy_star.png`、`enemy_bastion.png`：4 款去背透明敵機精靈。
- `assets/bosses/*.png`：全 11 款透明去背神話 Boss 圖資。
- `starfall-quiz.html`：重構 `#pauseScreen`，新增 `#pauseModal`、主動裝備槽、庫存清單與被動輔助裝置專區。
- `style.css`：新增暫停作戰武裝庫樣式（`.armory-slot-card`、`.armory-inv-card`、`.armory-passive-card`、主動/被動徽章）。
- `game.js`：
  - `Game.constructor`：初始化 `equippedActiveWeapons`、`baseShootTimer`、`bulletSlowFactor`、`moveSpeedMultiplier`，預載 4 種敵機精靈。
  - `bindEvents()`：滑鼠、觸控、鍵盤方向鍵與 WASD 嚴格邊界鎖定；綁定暫停視窗按鈕。
  - `fireBaseVulcan(dt)`：常駐雙聯電漿直射機砲。
  - `fireWeapons(dt)`：嚴格檢驗 `isWeaponActiveEquipped()`，主動武器限制同時最多裝備 3 款；被動武器自動生效。
  - `update(dt)`：每幀強制玩家座標 clamp 在畫布邊界；實裝 4 種敵機獨立 AI 行為與彈幕發射；彈幕速度受 `bulletSlowFactor` 影響；擊中觸發 Hit-stop、受擊白光與擊退。
  - `render()`：4 種敵機專屬精靈渲染（含旋轉與受擊白光濾鏡、重裝敵機血條）。
  - `renderPauseArmory()`：主動 3 槽裝備/卸下、主動庫存替換、被動裝置監控。
  - `openUpgradeScreen()` / `generateUpgradeChoices()` / `applyUpgrade()`：5 階強度對應、0 題生存補給、第 3 卡 30% 跳階機率計算與 Lv.5 排除機制。
- `test-v1.4.js`：包含 7 大核心需求、蒙地卡羅 10,000 次跳階驗證、圖資透明度檢測之自動化測試套件。

### 測試方式
1. 執行 `node test-v1.4.js`：
   - 驗證主動（9款）與被動（7款）分類正確。
   - 驗證戰機移動 Clamp 邊界 `[24, 416]` 與 `[30, 744]` 嚴格受限。
   - 驗證 5 階強度倍率（1.0x 至 5.0x）。
   - 驗證 0 題答對時僅提供 3 項生存補給且無任何武器選項。
   - 進行 10,000 次蒙地卡羅跳階機率測試，驗證第 3 張卡跳階率為 29.94%（符合 30% 規範）。
   - 驗證升滿 Lv.5 MAX 之武器不再進入升級選項。
   - 驗證主動武器上限 3 個限制。
   - 檢查 4 種敵機與 11 款 Boss 之 PNG 標頭與 RGBA 透明通道（Color Type = 6）。
2. 啟動本機伺服器 `http://localhost:8080/` 進行功能手動驗證：
   - 出擊後滑鼠任意拖曳至畫布外部，戰機平滑停在邊界內，不再超出畫面。
   - 不管切換何種首發武器，自機始終持續射出雙聯直射電漿子彈。
   - 波次出現 4 種外觀與彈幕各異之敵機（蛇行俯衝的紅翼機、懸停交叉射擊的碧甲機、自轉放射 6 芒刺的金星、以及發射巨型紫色電漿球的要塞重艦）。
   - 子彈擊中敵機與 Boss 時，可見明顯受擊白光、金屬火花噴濺與凍結幀阻滯感。
   - 按暫停開啟武裝庫，可看見已裝備之主動武器與「卸下」按鈕，並可在庫存中點選其他主動武器更換（上限 3 個），被動武器則常駐展示。
   - 答對不同題目數量進入升級，卡片強度標籤、倍率與第 3 張跳階機率均如實展現。
3. 預期結果：7 大核心需求全數高標準實現。
4. 實際結果：所有自動化測試全數通過，桌機目錄與桌面鏡像目錄完全同步。

---

## 2026-09-21 21:50 — BUILD-009：武器庫UX易用性重構、幽遊白書靈丸純音效與白光衝擊波、神話Boss華麗招式大招音效、6大真融合雙Rank 3解鎖與題庫去重複

### 完成項目
1. **暫停武裝庫 UX 易用性重構 (Pause Armory UX Redesign)**：
   - 徹底告別直列式大卡片壅塞感，採用 **48×48px 小圖示網格矩陣 (`armory-tiles-container`)** 呈現已獲取武器，並以高亮光環區分主動/被動/已裝備狀態。
   - 實裝 **即時戰術檢視面板 (`armoryInspector`)**：滑鼠懸停 (hover) 或點選 (click/tap) 任意圖示即刻無縫檢視武器全稱、分類標籤、當前階級 (Lv.1~Lv.5)、品質倍率加成、詳細戰術效果與融合提示。
   - 頂部保留 3 個精緻小巧主動裝備槽 (`active-slots-compact-grid`)，可直接一鍵卸下。
2. **幽遊白書風格靈丸純音效與純白光圈衝擊波**：
   - 徹底移除 Web Speech TTS 機械語音，改由純 Web Audio 程序化合成帶寬帶通濾波的指尖諧振嗡鳴 (`playSpiritCharge`, 950Hz → 2800Hz)。
   - **嚴格執行靜音規則**：若靈丸未蓄滿（非 MAX 階級且非彗星靈丸），放開發射時直接靜音 `return;`，嚴格不發出任何音效。
   - 滿蓄力（Tier 5）或彗星靈丸發射時：觸發經典幽遊白書高頻放電尖嘯 (`2800Hz → 180Hz`)、次低音重錘 (`140Hz → 24Hz`) 與白噪音爆風瞬態。
   - 靈丸命中時觸發純白擴散光圈衝擊波 (`reiganShockwaves`，半徑由 15px 擴散至 95px，純白 `#ffffff`，`shadowBlur: 24`)，伴隨 16 條星芒射線粒子與空間扭曲震顫。
3. **神話 Boss 專屬華麗機制與專屬大招音效**：
   - 每位 Boss 配備 3 種以上獨立攻擊模式：
     - **迦樓羅・裂空王**：神鳥飄羽（`feather` 羽毛彈幕帶正弦波動搖曳飛行）、一飛沖天（沿途留下一排浮空金羽，3.0 秒後原地引爆）、裂空金焰爪。
     - **雷公・震霄**：雷電五芒星陣（5 節點星芒連線隨後迸射）、天頂驚雷十連劈（10 道天降垂直落雷柱與閃電）、乾坤雙雷鼓結界（召喚左右雷鼓提供 50% 護盾）。
   - **Boss 大招專屬震撼音效**：預警時播放急速升頻警報 (`playBossUltimateWarning`)，爆發時觸發神話屬性雷爆與空間次低音震顫 (`playBossUltimateCast`)。
   - **Boss 生命值成長平滑化**：Stage 1~10 基礎 HP 調整為 `[3200, 4200, 5400, 6800, 8500, 10500, 12800, 15500, 18500, 22000]`，打造 25~45 秒兼具張力與流暢度的街機節奏。
4. **6 大真融合武器雙素材 Rank >= 3 覺醒機制**：
   - 當特定 2 款素材武器雙雙達到 Rank >= 3 時，答題升級 3 選 1 保證獻上金色傳奇【真・融合解鎖卡】。
   - 6 組融合全面對齊 16 款武器庫存：彗星靈丸、虹晶天幕、蜂群獵手、軌道壁壘、熔核轟擊、凝時裁決。
5. **自適應題庫去重複化**：
   - 已答對題目權重由 4.0 大幅壓低至 0.05（出現率 < 1%），未作答新題提高至 6.0，答錯未復仇題目提升至 15.0，大幅避免題目頻繁重複。

### 異動檔案
- `starfall-quiz.html`：重構 `#pauseModal`，新增 `.active-slots-compact-grid`、`.armory-interactive-layout`、`.armory-tiles-container`、`#armoryInspector`。
- `style.css`：新增小圖示網格樣式（`.armory-tile`、`.tile-rank-badge`、`.tile-equipped-dot`）、檢視面板樣式（`.armory-inspector-panel`、`.inspector-header`、`.inspector-fusion-hint`）與緊湊裝備槽樣式。
- `data/fusion-data.json`：校準 6 大融合素材武器配方與雙 Rank >= 3 條件。
- `game.js`：
  - `SoundManager`：新增 `playSpiritCharge`、`playSpiritFire`（未滿蓄力嚴格靜音）、`playReiganImpact`、`playBossUltimateWarning`、`playBossUltimateCast`；將 `speak()` 完全靜音。
  - `Game`：新增 `reiganShockwaves` 衝擊波實體管線、`createReiganShockwave()`、羽毛彈幕正弦飄動與 3 秒浮空金羽自爆機制。
  - `Boss`：實裝迦樓羅（神鳥羽刃/一飛沖天浮空羽/金焰爪）、雷公（五芒星陣/十連落雷/乾坤雷鼓結界）；調整 Boss 基礎 HP 數值。
  - `Upgrade`：實裝雙素材 Rank >= 3 之真融合卡片生成邏輯。
  - `DataStore`：`pickAdaptiveQuestions` 配置 0.05/6.0/15.0 權重，徹底壓低答對題目重複率。
  - `Armory`：實裝 `renderPauseArmory()` 與 `renderArmoryInspector()`，支援滑鼠懸停與點選即時切換。
- `test-v1.5.js`：全新 v1.5 全自動測試套件（5 大核心模組校驗）。

### 測試方式
1. 執行 `node test-v1.5.js`：
   - 驗證靈丸未集滿嚴格靜音、TTS 已被完全阻斷、純白光圈衝擊波邏輯。
   - 驗證武器庫 48x48 緊湊網格與檢視面板 HTML/CSS/JS 綁定。
   - 驗證素材未達 Rank 3 時不提供融合，素材達 Rank 3 時正確提供【真・融合解鎖】。
   - 驗證迦樓羅正弦羽毛與 3.0s 浮空金羽引爆、雷公五芒星陣與十連落雷、平衡後之 Boss HP 曲線。
   - 進行 10,000 次蒙地卡羅選題測試，確認已答對題目的抽中率僅 0.70%（嚴格 < 1%）。
2. 執行 `node test-v1.4.js` 與 `node test-all-features.js`：確認向下相容性 100% 通過。
3. 預期結果：5/5 全新模組與 7/7 既有規格測試全數通過。
4. 實際結果：所有測試順利通過，已同步所有變更至桌面專案目錄。

---

## 2026-09-21 22:30 — BUILD-010：神話機神史詩躍進（專屬 40 秒 Procedural BGM、神格破滅遺言、階級視覺質變、HP平衡重構、造型質變、具名大招、獨立移動演算法與神話剋制彩蛋）

### 完成項目
1. **關卡專屬 40 秒 Procedural BGM（十大神話主題、管樂、Formant人聲和音）**：
   - 於 `BgmEngine` 為全十關 Boss 量身打造專屬程序化 Web Audio 合成音軌（120 BPM、80 拍、320 步 16 分音符，精確 40.0 秒無縫迴圈）。
   - 具備民族風格笛音/管樂（`playFlute`：竹笛、日本尺八、希臘雙簧管 Aulos、泰坦號角）與雙帶通濾波器人聲共振峰合成器（`playChoirFormant`：'Aah'/'Ooh' 塞壬女妖、荒古喉音僧侶頌唱、奧林匹斯天使合唱、宇宙終焉安魂曲）。
   - 包含民族彈撥弦樂（`playPluck`：西塔琴 Sitar、日本箏 Koto、古希臘豎琴 Harp）與特色敲擊樂（太鼓、赫菲斯托斯打鐵砧高頻金屬泛音）。
   - 於 `startNewGame` 與 `closeUpgradeScreen` 自動綁定並切換當前關卡專屬音軌。
2. **Boss 敗北神格破滅專屬遺言與消散音效**：
   - 於 `data/boss-data.json` 為全十位 Boss（迦樓羅、雷公、美杜莎、饕餮、阿特拉斯、雅典娜、許德拉、獨眼巨人、玉藻前、提亞瑪特）及先鋒小 Boss 配置深刻故事背景之敗北台詞（如迦樓羅：「你竟然刮傷了我美麗的羽毛...難道是龍族派你來的嗎？！」）。
   - 前端新增 `#bossDefeatOverlay` 專屬遺言展示卡片，並透過 `playBossDefeatQuote` 激發各 Boss 專屬之神性破滅消散音效（狂風金鈴、電磁失能雜訊、神聖消散和弦鐘鳴）。
3. **16 款武器 Rank 1~5 肉眼可見階級進化**：
   - `Bullet` 實裝階級色彩層次：Rank 1 靈能天藍 (`#38bdf8`)、Rank 2 翡翠耀綠 (`#4ade80`)、Rank 3 虛空幽紫 (`#a855f7`)、Rank 4 熾烈熔橙 (`#fb923c`)、Rank 5 弒神金曜 (`#facc15`)。
   - 彈道視覺動態升級：Rank 2 附帶微粒光暈拖尾；Rank 3 附帶烈焰殘影光尾；Rank 4 環繞動態高頻電弧；Rank 5 MAX 爆發弒神金曜星芒十字光暈。
   - 命中火花粒子（`createHitSparks`）數量、射速、半徑與高階爆破衝擊波隨 Weapon Rank 階梯式放大。
4. **Boss 強度與血量曲線重構（30 秒換型態基線）**：
   - 以玩家單持 3 階武器與基礎機砲之平均火力（~2000 DPS）為基準，重新校準 Boss 總血量：
     - Stage 1（迦樓羅）：120,000 HP（第一階段 60,000 HP，耗時恰約 30 秒進入變身）。
     - Stage 9（玉藻前）：240,000 HP（基準 2 倍）。
     - Stage 10（提亞瑪特）：480,000 HP（基準 4 倍），且擁有完整的 **3 個戰鬥階段**（100% -> 66% -> 33% -> 0%）！
5. **Boss 階段重大造型質變（體型放大、神羽、符印光環、眼部耀斑）**：
   - 體型動態質變：Phase 1 基準 165px，Phase 2 放大至 205px (+25%)，Phase 3 放大至 245px (+50%)，碰撞體積同步等比縮放。
   - 動態程序化能量神翼（`renderBossEnergyWings`）：Phase 2 展現左右各 4 瓣高頻拍擊的能量光羽；Phase 3 展開四重宇宙神聖羽翼，伴隨星塵碎屑。
   - 旋轉符印光環與放電電弧（`renderBossAuras`）：Phase 2 虛線符印光環；Phase 3 雙重逆向宇宙光環與高壓雷漿弧線。
   - 機神眼部光束耀斑與動力爐光芒（`renderBossOpticFlares`）：高能反應爐呼吸式脈衝，第二型態啟動橫向光學變形拉絲光束（Anamorphic Visor Flare）。
   - 第二型態（深紅狂暴濾鏡）與第三型態（宇宙混沌神性金紫濾鏡）著色質變。
6. **入場與大招完全具名化（零泛稱）**：
   - 徹底消滅「神話機神」、「神威爆發」等任何泛稱。
   - 登場標題一律顯示：`第 ${boss.stage} 關【${boss.name}】降臨`。
   - 大招預警橫幅一律顯示：`【${bossName}】發動神話大招：${skillName}！`。
7. **十大 Boss 獨立移動演算法與硬直癱瘓**：
   - 迦樓羅：Lissajous 8 字型滑翔與高空盤旋。
   - 雷公：Z 字型疾雷折線平移，每 3.2 秒引發雷殛閃現殘影。
   - 美杜莎：蛇形 S 曲線連續複擺游弋。
   - 饕餮：重力深陷，周期性緩慢向中心下壓沉落。
   - 阿特拉斯：天穹重磅下墜蓄力，隨後發動震撼地面的泰坦重踏。
   - 雅典娜：軍事正三角巡弋陣型，頂點、右翼、左翼精確向量機動。
   - 許德拉：九頭蛇身劇毒波狀連續起伏擺動。
   - 獨眼巨人：鋼鐵重步梯形前進（Iron Step Marching）。
   - 玉藻前：天狐幻境魅影穿梭，周身附帶幻象 Shimmer 頻閃。
   - 提亞瑪特：原初混沌宇宙橢圓軌道旋轉，第三型態擴展為狂暴混沌大星軌。
   - 硬直癱瘓機制：受到剋制武器打擊觸發 `stunTimer`，Boss 停止一切移動與招式施展，身上迸射癱瘓電漿。
8. **神話暗線剋制彩蛋機制（Secret Mythic Weakness Counters）**：
   - 迦樓羅 vs 虛空重力奇點：雙翼受黑洞牽引失速，墜地失能硬直 3.5 秒。
   - 雷公 vs 青玉風雷飛輪：金屬刃切斷連鎖雷鼓，電容短路重創 8% 生命並癱瘓 3 秒。
   - 美杜莎 vs 玄冰凌柱：蛇髮群妖受到玄冰急凍，進入 4 秒冬眠狀態，全場蛇彈瞬間蒸發。
   - 饕餮 vs 熾陽熔岩榴彈：吞食高爆熔岩核引發腹腔內爆，重創 10% 生命且傀儡全滅。
   - 雅典娜 vs 五行太極陣盤：五行陰陽生剋破陣，埃癸斯神盾永久破滅碎裂。
   - 獨眼巨人 vs 裂變音浪重砲：聽覺中樞共振震裂，神鐵熔爐冷卻熄火 4 秒。
   - 玉藻前 vs 金陽聚焦光束：神光照破天狐真身，全數幻象分身當場蒸發幻滅。
   - 成功觸發彩蛋播放神聖上行琶音音效（`playSecretCounterTrigger`）與專屬金色 Toast 提示。

### 異動檔案
- `data/boss-data.json`：配置 Stage 1~10 基礎血量（Stage 1 120,000、Stage 9 240,000、Stage 10 480,000）、敗北遺言 `defeatVoiceLine`、完全具名化大招清單。
- `starfall-quiz.html`：新增 `#bossDefeatOverlay` 敗北神格破滅遺言卡片容器與文字節點。
- `style.css`：新增 `#bossDefeatOverlay`、`.boss-defeat-card`、金黃神聖邊框與暗幕動畫樣式。
- `game.js`：
  - `BgmEngine`：全面重構為 10 首區域主題程序化曲目（320 步 40 秒循環），實裝竹笛/管樂、共振峰 Formant 合唱與彈撥樂器。
  - `SoundManager`：新增 `playBossDefeatQuote` 與 `playSecretCounterTrigger`。
  - `Bullet`：實裝 `rank` 與 `rankColors`，更新 16 款武器發射傳參。
  - `createHitSparks`：支援武器階級粒子層次與衝擊波。
  - `render`：子彈階級粒子尾焰、電弧、星芒；Boss 階段動態神翼、光環、眼部耀斑與濾鏡。
  - `updateBoss`：實裝 10 大獨立移動演算法與硬直癱瘓機制。
  - `damageBoss`：實裝 3 階段轉換、神話暗線剋制判定 `checkBossMythicWeakness`、敗北遺言觸發。
  - `showUltimateWarning`：全面具名化。
- `test-v1.6.js`：全新 v1.6 全自動化測試套件（8 大核心機制 100% 驗證）。

### 測試方式
1. 執行 `node test-v1.6.js`：驗證 8 大史詩機能 100% 通過。
2. 執行 `node test-v1.4.js` 與 `node test-v1.5.js`：驗證所有歷史規格 100% 相容。
3. 啟動本機伺服器在 `http://localhost:8080/` 實機測試，各關卡音樂、Boss 動作、羽翼、敗北台詞與彩蛋剋制皆完美運作。

---

## 2026-09-21 23:30 — BUILD-011：視覺與戰鬥美學全面昇華（Boss AI 專屬元素特效貼圖、前哨神械十魔王巡迴、靈丸防多幀秒殺與數值平衡、武器庫 3 槽雙行排版與自動裝備、衝撞型/自爆型新雜兵、全 10 關 9:16 垂直神話背景平滑滾動）

### 完成項目
1. **Boss 元素特效 AI 量身打造（7 款透明 PNG 貼圖）**：
   - 徹底淘汰幾何方塊與圓圈，全面導入高質感透明通道特效貼圖：
     - `fx_lightning.png`：高能雷霆電漿電弧（雷公落雷、電擊彈幕）。
     - `fx_fire_meteor.png`：熾烈流星火球與熔岩烈焰（饕餮噴火、獨眼巨人天爐熔岩）。
     - `fx_glacial_crystal.png`：玄冰晶刺稜柱（美杜莎石化光束、玄冰凌柱）。
     - `fx_golden_feather.png`：裂空神鳥金羽光刃（迦樓羅羽刃、浮空引爆羽）。
     - `fx_void_blackhole.png`：虛空引力黑洞漩渦（提亞瑪特混沌黑潮、奇異點核心）。
     - `fx_holy_spear.png`：神聖制裁金矛（雅典娜貫穿長矛）。
     - `fx_toxic_acid_orb.png`：蝕骨劇毒酸球（許德拉多頭蛇劇毒彈幕）。
   - 彈道與特效渲染管線導入 `globalCompositeOperation = 'lighter'` 加色混合，呈現絢麗奪目的神話機神光效！
2. **前哨神械 (Mini-Boss) 十魔王招式巡迴武裝投影**：
   - 解除對第一關迦樓羅羽毛彈幕的固定套用，實裝 `executeMiniBossAttack(boss)`。
   - 根據 `(boss.miniCycle - 1) % 10` 依序巡迴釋放全 10 關主要魔王之標誌性招式投影（風刃、落雷、蛇鏡、噬火、巨岩、金矛、酸泡、星火、妖火、創世混沌），並觸發專屬投影宣告橫幅。
3. **靈丸數值平衡與防多幀秒殺機制**：
   - 基礎傷害下修為 85（搭配階級倍率 `[0, 1.0, 1.6, 2.4, 3.4, 4.8]`），穿透數上限設為 12。
   - 碰撞判定實裝 `lastHitBossTime` 0.22 秒時間戳冷卻鎖，徹底杜絕單發靈丸穿過大體積 Boss 碰撞區造成數十幀重複傷害的秒殺問題，使 16 款武器系統始終保有核心養成價值。
4. **武器庫面板 UX 全面重構**：
   - 點選未裝備之主動武器時，若主動槽未滿 3 款，**直接自動裝入第一個空槽**並彈出 Toast 反饋。
   - 武裝庫存結構與視覺拆分為「⚔️ 主動戰鬥武裝」與「🛡️ 被動增益模組」兩大獨立區塊。
   - 3 個已裝備槽位採用 3 欄等寬緊湊網格（`repeat(3, 1fr)`），上下垂直雙行佈局（第一行展示武器完整全名，第二行並列顯示等級 `Lv.X` 與紅底【卸下】按鈕），移除「主動槽 01」冗贅贅字，免拖曳一覽無餘！
5. **新型衝撞型與自爆型雜兵及精緻彈道**：
   - `enemy_charger.png`（赤隼衝撞機）：進場後鎖定自機 X 軸發射危險預警線，0.45 秒後以 480px/s 極速俯衝撞擊。
   - `enemy_bomber.png`（核芯自爆機）：受重創或逼近自機 125px 觸發 0.6 秒自爆警報紅光閃爍，隨後劇烈殉爆釋放 8 向破片彈幕。
   - 巡弋飛彈（`homing`）渲染管線重構為高細節流線型導彈彈體、紅合金彈頭、穩定側翼與動態推進烈焰尾噴。
6. **全 10 關 9:16 垂直神話背景與平滑滾動系統**：
   - Stage 1 採用使用者提供之縱向 9:16 浮空天宮星河圖，其餘 9 關配置專屬風格之神話背景。
   - `renderStageBackground(ctx)` 實裝雙層無縫垂直視差滾動（`ctx.drawImage(bgImg, 0, bgY - H, W, H)` 與 `ctx.drawImage(bgImg, 0, bgY, W, H)`），營造戰機穿梭於雲海天宮與宇宙星艦的史詩沉浸感。

### 異動檔案
- `assets/fx/`：新增 7 款透明通道特效貼圖（`fx_lightning.png` 等）。
- `assets/enemies/`：新增 2 款新雜兵貼圖（`enemy_charger.png`, `enemy_bomber.png`）。
- `assets/backgrounds/`：新增 10 大關卡 9:16 垂直背景貼圖（`bg_stage_1.jpg` ~ `bg_stage_10.jpg`）。
- `scripts/process_assets.py`：AI 特效通道摳圖、雜兵切圖與背景裁切轉換腳本。
- `starfall-quiz.html`：重構武器庫主動槽位結構與主/被動分區容器。
- `style.css`：新增 `.active-slots-compact-grid`、`.armory-slot-compact` 雙行排版與分區次標題樣式。
- `game.js`：
  - `loadAssets`：註冊載入全套 FX、新雜兵與 10 關垂直背景。
  - `renderStageBackground`：實裝垂直雙層無縫滾動。
  - `executeMiniBossAttack`：實裝前哨神械十魔王投影巡迴。
  - `releaseSpiritCharge` & `damageBoss`：實裝靈丸威力平衡與 0.22 秒防多幀秒殺判定鎖。
  - `renderPauseArmory`：實裝未裝備主動武器點擊自動裝入空槽、雙行槽位與主被動分流。
  - `spawnMobWave` & `updateEnemies`：實裝 `charger` 俯衝與 `bomber` 自爆。
  - `renderBullets`：實裝 FX 貼圖加色渲染與流線型巡弋飛彈繪製。
- `test-v1.7.js`：全新 v1.7 視覺美學與戰鬥機制全自動化測試套件。

### 測試方式
1. 執行 `node test-v1.7.js`：6/6 大美學與機制模組 100% 驗證通過。
2. 執行 `node test-v1.4.js; node test-v1.5.js; node test-v1.6.js; node test-all-features.js; node test-weapons-boss.js`：全部通過，向下相容性 100%。
3. 同步 scratch 變更至桌面專案目錄 `C:\Users\何任軒\Desktop\Antigravity\starfall-quiz\`，並在桌面目錄重新執行 `test-v1.7.js` 驗證無誤。

---

## 2026-09-22 08:30 — BUILD-012：各關卡專屬 BGM、超音速飛行粒子、16 款武器全控 LAB、子彈專屬音效分化與乾淨戰鬥畫面

### 完成項目
1. **各關卡專屬高品質 BGM 循環播放（10 首 MP3）**：
   - 整合 `Desktop/Antigravity/Artifact` 之 10 首專屬 BGM 至 `assets/audio/bgm/`。
   - `BgmEngine` 升級為優先播放實體關卡 MP3 音軌，支援跨關卡淡入淡出切換、無縫循環播放與 Web Audio 混音合成兜底備案。
2. **無縫神話天宮天際線與 3 層超音速飛行粒子**：
   - 改善背景貼圖捲動邊界感，中央固定天宮核心，並加入 3 層速度/大小/透明度分化的高速向下穿梭飛行粒子，營造劇烈俯衝前進感。
3. **戰鬥內 LAB 面板全 16 款武器控制**：
   - 全面擴充至完整 16 款神話武器型錄（`STARFALL_WEAPONS_CATALOG`），每一款武器皆可自由裝備、卸下與 R0~R5 階級微調。
4. **武器射擊音效全面分化（11 組獨立合成器）**：
   - 全面擺脫單一雷射啾啾聲，為機砲、光束、靈丸、穿甲鏢、導彈、飛輪、榴彈、冰柱、音浪砲、太極陣、仙泉打造 11 組獨一無二的音效。
5. **乾淨戰鬥畫面（浮動傷害數字抑制）**：
   - 關閉畫面中密集跳出的傷害浮字，大幅提升畫面清爽度與視野專注度。

---

## 2026-09-22 09:30 — BUILD-013：玄天冰柱改被動、折射星核/多段武器抗卡頓、全武器傷害威力重平衡、Boss 召喚物與爆裂彈 AI 精靈貼圖全面替換

### 完成項目
1. **玄天冰魄凌柱 (`cryo_spire`) 全面改為被動武器**：
   - 依使用者明確反饋：冰柱乃自畫面頂端隨機墜落之範圍打擊機制，無須玩家瞄準，正式劃為**被動增益模組**（`slot: "passive"`, `isPassive: true`）。
   - 移出 3 格主動裝備槽限制，不再擠佔機頭直射主動武裝。
   - 武器庫中點選自動裝備至被動槽；戰鬥中只要取得/升級，即自律從天際召喚永凍冰錐。
2. **折射星核 (`prism_wingman`) 與多段傷害武器抗卡頓徹底解決 (Anti-Stutter Pattern)**：
   - **問題根源**：原先 `prism_wingman` 在每幀 (60 FPS) 遍歷敵機並每秒調用 120 次 `damageBoss`，造成嚴重的 CPU 尖峰與幀率掉落。
   - **解法實裝**：
     - **0.15s 離散計時結算 Tick**：視覺射線維持 60 FPS 平滑連接與動態追蹤，但尋標與傷害/粒子運算降低 88%，徹底根除卡頓！
     - **目標快取機制 (Target Caching)**：保留上次鎖定目標，避免每幀 O(N^2) 重複尋標。
     - **熔岩地熱領域 (`lavaPools`)**：加入 `0.20s` 離散計時結算，避免多個熔岩領域每秒對 Boss 與小兵產生上百次碰撞調用。
     - **金陽光束砲 (`beam_cannon`)**：發射計時器由 `0.07s` 調升至 `0.14s`，單發傷害乘以 2，避免彩虹融合下密集光束重疊引起的瞬間掉幀。
3. **16 款武器威力大幅提升與階級成長重平衡**：
   - 多管神機砲 (`multishot`)：基礎傷害 28 → 36，每級成長 +9 → +12。
   - 金陽聚焦光束 (`beam_cannon`)：單發傷害 24 → 50，每級成長 +10 → +22。
   - 靈能聚變核心 (`spirit_bullet`)：基礎傷害 110 → 130，每級成長 +45 → +50。
   - 超空泡穿甲鏢 (`kinetic_dart`)：基礎傷害 55 → 68，每級成長 +18 → +22。
   - 烈陽核融導彈 (`homing_missile`)：基礎傷害 48 → 58，每級成長 +14 → +18。
   - 青玉風雷飛輪 (`jade_chakram`)：基礎傷害 60 → 80，每級成長 +20 → +26。
   - 神鳥天翼僚機 (`combat_wingman`)：僚機飛針傷害 24 → 32，每級成長 +8 → +10。
   - 虹光折射星核 (`prism_wingman`)：DPS 220 → 260，每級成長 +90 → +110。
   - 熾陽熔岩噴射核 (`grenade_launcher`)：重砲傷害 95 → 130，每級成長 +40 → +48。
   - 虛空重力奇點 (`singularity_core`)：引力傷害 70 → 95，每級成長 +30 → +38。
   - 五行太極陣盤 (`taiji_array`)：單發 50 → 65，終極混元 60 → 75。
   - 玄天冰魄凌柱 (`cryo_spire`)：單柱傷害 120 → 160，每級成長 +45 → +55。
   - 裂變音浪重砲 (`sonic_cannon`)：巨型音浪 95 → 135，每級成長 +38 → +48。
4. **Boss 召喚物與爆裂彈 AI Sprite 全面重製（告別粗糙圓圈散開！）**：
   - **痛點解決**：徹底移除所有 `ctx.arc(); ctx.fill();` 粗糙陽春平塗圓圈，全數替換為高解析度 AI 繪製之專屬透明通道 Sprite：
     - `minion_ambrosia_flask.png`：奧林帕斯神聖甘露仙瓶（金樽仙露、懸浮光暈與倒數進度環）。
     - `minion_hydra_head.png`：許德拉劇毒蛇首要塞（墨綠毒牙、蛇身擺動與酸液光暈）。
     - `minion_thunder_drum.png`：雷公乾坤雷紋戰鼓（雷部金紋、電光火石與戰鼓旋轉光環）。
     - `minion_taotie_meat.png`：饕餮貪食血肉核心（暗紅血肉之核、呼吸心跳脈動）。
     - `minion_gorgon_shadow.png`：美杜莎紫晶蛇影殘像（紫晶蛇髮、半透明靈體幽光）。
     - `minion_titan_pillar.png`：阿特拉斯擎天重力星柱（厚重泰坦星柱、重力扭曲漣漪）。
     - `minion_garuda_viper.png`：迦樓羅風神翠玉翼蛇（翠綠羽翼、靈活遊隼動態）。
     - `fx_rock_shard.png`：阿特拉斯巨岩隕石（44px 旋轉巨岩）與爆裂碎石（24px 定向尖銳破片）。
     - `fx_cluster_bomb.png`：核芯自爆機高爆集群破片（26px 烈焰高爆破片）。
     - `fx_feather_shard.png`：金羽爆裂剃刀光刃（28px 金羽破片）。
   - **常規敵彈美學昇華**：常規子彈升級為**動態定向氣動能量光梭**（流線型漸層尾曳、高光聚能核心），徹底終結「滿螢幕都是粉紅/橘黃大圓圈」的劣質觀感！
   - 召喚物增加精緻戰術 HUD：專屬 emoji 標籤、黑色描邊高對比文字與雙層生命條。

### 異動檔案
- `assets/minions/`：新增 7 款專屬神話召喚物 AI Sprite。
- `assets/fx/`：新增 3 款爆裂尖銳碎塊與高爆集群破片 AI Sprite。
- `data/weapon-data.json`：更新 `cryo_spire` 為被動槽位（`slot: "passive", isPassive: true`）。
- `game.js`：
  - `loadAssets`：預載註冊 10 款全新 Sprite。
  - `STARFALL_WEAPONS_CATALOG`：更新 `cryo_spire` 被動標籤與全武器基數。
  - `fireWeapons`：`cryo_spire` 改為自律被動發射；`beam_cannon` 頻率降為 0.14s。
  - `updatePrismWingman`：實裝 0.15s 離散計時 Tick 與目標快取抗卡頓管線。
  - `lavaPools` 更新：實裝 0.20s 離散計時結算。
  - 武器發射函式：全量武器傷害調校。
  - `renderBullets`：全面導入 `fx_rock_shard`、`fx_cluster_bomb`、`fx_feather_shard` 與氣動能量光梭。
  - `renderBossMinions`：全量導入 7 款專屬 Sprite、HUD 倒數外環與血條。
- `test-v1.9.js`：全新 v1.9 全自動化驗證測試套件。

### 測試方式
1. 執行 `node test-v1.9.js`：8/8 大驗證項目全數 PASS 通過。
2. 執行 `node test-v1.4.js; node test-v1.5.js; node test-v1.6.js; node test-v1.7.js; node test-v1.8.js`：全數 PASS 通過，零功能回退。
3. 同步至 `C:\Users\何任軒\Desktop\Antigravity\starfall-quiz\` 桌面工作區，並於桌面目錄執行 `node test-v1.9.js` 驗證 100% 通過。

---

## 2026-09-22 10:28 — BUILD-014：武器稀有度權重抽取、音波多重打擊頻率校正、Boss 召喚物透明通道清理、縱向雷電與學員 S0000 序號自動配發系統

### 完成項目
1. **反向傷害加權三選一武器抽取機制 (稀有度/數值平衡)**：
   - 根據使用者需求「傷害力越高的武器，出現在三選一的機率要下降」：
   - 於 `generateUpgradeChoices` 將原有的均勻隨機打亂洗牌改為**傷害反比加權無放回抽樣**：
     $$Weight(w) = \left( \frac{100}{\max(30, \text{baseDamage})} \right)^{1.4}$$
   - 實測 20,000 次模擬：低傷害基礎武裝（連裝脈衝砲 30 dmg）出現率約 19.7%，高傷害重型武裝（裂變音浪重砲 135 dmg）出現率降至 3.2%，出現機率差距超過 6.1 倍，完美實現高階重砲的稀有感與平衡度。
2. **裂變音浪重砲多重碰撞頻率校正 (杜絕秒殺 Bug)**：
   - **痛點根治**：音浪判定半徑達 95px、穿透值 99，以 520 px/s 移動時在 60 FPS 下與大型 Boss 重疊長達 15~25 幀，造成單發音浪每幀連續扣血（單發造成高達 2000~3000 傷害，瞬間秒殺敵人與 Boss）。
   - **校正機制**：
     - 一般雜兵：新增 `b.hitEnemies = new Set()`，單一發音浪對同一雜兵僅造成一次 135 傷害。
     - Boss 召喚物：新增 `b.hitMinions = new Set()`，確保單發音浪對單一召喚物僅結算一次傷害。
     - Boss 本體：加入 `b.lastHitBossTime`（0.35s 頻率鎖），使音浪掠過 Boss 時至多造成 1~2 次傷害（135 ~ 270 總傷），傷害曲線精準符合預期武器配置。
3. **Boss 召喚物與破片 AI 貼圖黑底完全透明化 (100% Alpha Channel)**：
   - 撰寫 `scripts/remove_black_bg.py`，精準消除 10 款 AI 生成 Sprite 的黑色背景，邊緣平滑羽化過渡至純透明 RGBA：
     - `assets/minions/minion_ambrosia_flask.png`
     - `assets/minions/minion_hydra_head.png`
     - `assets/minions/minion_thunder_drum.png`
     - `assets/minions/minion_taotie_meat.png`
     - `assets/minions/minion_gorgon_shadow.png`
     - `assets/minions/minion_titan_pillar.png`
     - `assets/minions/minion_garuda_viper.png`
     - `assets/fx/fx_rock_shard.png`
     - `assets/fx/fx_cluster_bomb.png`
     - `assets/fx/fx_feather_shard.png`
   - 10 款貼圖四角經程式驗證均為 RGBA 透明（Alpha = 0），告別任何黑底方框破綻。
4. **雷電攻擊縱向貫穿與拉長呈現 (縱向雷光神槍)**：
   - 將 `assets/fx/fx_lightning.png` 逆時針旋轉 44° 並精確裁切為純垂直縱向圖片（尺寸 764x1274，長寬比超過 1.66:1）。
   - 在 `game.js` 的 `renderBullets` 中，更新 `thunder` 與 `thunder_bolt` 的彈幕渲染幾何角度為 `Math.atan2(eb.vy, eb.vx) - Math.PI / 2`，確保雷光方向沿軌道垂直縱向指向目標。
   - 依據需求實裝**縱向拉長比例**：天劫重雷槍拉長至 `28x112px`，常規疾電弧拉長至 `24x60px`，雷電劈落之震撼氣勢大幅提升。
5. **學員身分登錄與 Google Sheet "S0000" 自動新序號分配系統**：
   - **前端開始介面**：於 `starfall-quiz.html` 增設「🧑‍✈️ 駕駛員學員身分登錄」卡片，包含姓名輸入框（`#studentNameInput`）、年級下拉選單（`#studentGradeSelect` 一至六年級與國中）、即時序號徽章（`#studentIdBadge`）與雲端狀態列。
   - **Google Apps Script 後端 (`apps-script/Code.gs`)**：
     - 新增 `action: 'register_student'` 端點。
     - 實作 `registerStudent_(payload)`：於 `Students` 工作表查詢同名同級學員，若無則遍歷現存學號最大數值，自動產生 `S0000` 格式新學號（如 `S0001`、`S0002`...），記錄創建時間並如實追加至試算表。
   - **遊戲端整合 (`game.js`)**：
     - `DataStore` 實作 `syncStudentProfile(name, grade)`，支援本機 localStorage 序列號與 Google Sheet API 雙重同步。
     - 點擊「出擊開始戰鬥」時自動校驗並建立/綁定學號，所有作答歷程（`recordAttempt`）皆如實附帶該 `student_id`。

### 異動檔案
- `assets/minions/`（7 款 PNG 貼圖黑底完全透明化）
- `assets/fx/`（`fx_rock_shard.png`, `fx_cluster_bomb.png`, `fx_feather_shard.png` 黑底透明化；`fx_lightning.png` 縱向旋轉與透明化）
- `scripts/remove_black_bg.py`（黑底去除與透明通道重建腳本）
- `apps-script/Code.gs`（新增 `register_student` 端點與 S0000 序號自動配發寫入邏輯）
- `starfall-quiz.html`（增設學員身分登錄卡片與輸入組件）
- `game.js`：
  - `generateUpgradeChoices`：反向傷害加權抽樣演算法。
  - `fireSonicCannon` & 碰撞檢測：`hitEnemies`、`hitMinions` 與 `lastHitBossTime` (0.35s) 傷害頻率鎖。
  - `renderBullets`：雷電貼圖縱向角度校準與 112px 縱向拉長渲染。
  - `DataStore`：`syncStudentProfile`、本機與雲端 S0000 序號產生、作答記錄學員 ID 綁定。
  - UI 事件綁定：學員姓名年級變更即時同步與出擊自動登錄。
- `scripts/test-v1.10.js`：BUILD-014 專用自動化測試套件（5 大核心測試全數 PASS）。

### 測試方式
1. 執行 `node scripts/test-v1.10.js`：
   - 11 款 Sprite 透明度與縱向尺寸測試：PASS。
   - 20,000 次反向加權抽取統計：低傷武器 (30 dmg) 機率 19.71% vs 高傷音浪 (135 dmg) 機率 3.16%，相差 6.24 倍，完美 PASS。
   - 音浪多幀碰撞計時鎖測試：普通敵機 20 幀重疊僅命中 1 次；Boss 60 幀（1秒）受擊限制 3 次（405傷），完美 PASS。
   - S0000 序號正則與遞增產生演算法：`[] -> S0001, [S0001..S0015] -> S0016, [s0099] -> S0100`，完美 PASS。
   - HTML、game.js 與 Code.gs 欄位端點一致性：PASS。
2. 同步至 `C:\Users\何任軒\Desktop\Antigravity\starfall-quiz\` 桌面工作區，並於桌面目錄執行 `node scripts/test-v1.10.js` 驗證 100% 通過。

---

## 2026-09-22 11:15 — BUILD-015：學生錯題與出題策略完全獨立、S0000 學號名冊正規登記、平板與手機版自適應建置

### 完成項目
1. **每個學生的錯題與出題策略 100% 嚴格獨立隔離**：
   - 解決痛點：原本學員錯題進度為全域單一 Map，不同學生登入時會繼承上一位學生的錯題（弱點復仇池被污染）或精熟題目（新學生選不到已被做過的題目）。
   - 架構重構：
     - `DataStore.allStudentProgress` 改為雙層字典：`allStudentProgress[student_id][question_id]`。
     - 升級 IndexedDB 至 v2：新增 `student_progress` ObjectStore，使用複合鍵 `${student_id}::${question_id}` 並建立 `student_id` 專屬索引。
     - 新增 `getStudentProgressMap(studentId)`、`getMistakeCount(studentId)`、`switchStudent(studentId, name, grade)`。
     - `pickAdaptiveQuestions` 嚴格依據當前選定學員之獨立歷程評分：新學員錯題數為 0，所有題目皆為權重 6.0 之全新推薦題目；學員 A 答錯之弱點題目（權重 15.0）絕不流入學員 B 的題池。
     - 徹底修正 `loadFromGoogleSheet` 載入試算表名冊時無差別將當前學號覆寫為 `students[0]`（測試玩家）的致命 Bug。
2. **S0000 流水號格式嚴格綁定 Student ID，輸入姓名綁定 display_name**：
   - `Code.gs`（Google Apps Script）：
     - `registerStudent_` 嚴格將自動生成的 `S0000` 序號寫入 `student_id` 欄位，輸入姓名寫入 `display_name` 欄位，年級寫入 `grade` 欄位，並將 `active` 設為 `TRUE`，記錄至 `Students` 工作表。
     - `doGet` 新增 `action=student_progress` 端點（`getStudentProgressData_`），能精確依傳入的 `student_id` 統計並回傳該學員在 `Attempts` 表的所有作答錯題歷程。
   - 前端登入 UI：
     - 新增 `#studentSelect` 學生選擇下拉選單，自動列出試算表名冊與本機已登記學生。
     - 提供「➕ 註冊新駕駛員學員...」選項，輸入姓名與年級即可一鍵配發新序號並自動登記至雲端與本機名冊。
     - 動態顯示「待雪恥錯題：X 題」，清楚反饋當前學生的獨立弱點狀態。
3. **建置平板與手機可完美執行的行動端版本**：
   - **PWA 漸進式網頁應用支援**：
     - 新建 `manifest.json`，配置 `display: standalone`、`orientation: portrait`、主題色 `#060912` 與高解析圖示。
     - 於 `index.html` 與 `starfall-quiz.html` 增設 `<meta name="viewport" content="...viewport-fit=cover">`、`<meta name="apple-mobile-web-app-capable" content="yes">` 等行動端全螢幕標頭。
   - **多設備智慧自適應視圖 (`fitStage`)**：
     - 手機直向（寬度 <= 600px 且高寬比 >= 1.35）：自動採用 100% 滿版無黑邊，徹底消除手機上下黑邊問題。
     - 平板直向（寬度 <= 1024px 且高寬比 >= 1.15）：採用約 0.64 舒適比例（如 iPad 691px x 1080px），兼顧視野與雙手握持觸控舒適度。
     - 桌面橫向：保持經典 9:16 街機框置中。
     - 支援 `window.visualViewport` 與 `orientationchange` 動態縮放。
   - **觸控體驗與全螢幕操作提升**：
     - 指針事件加入 `setPointerCapture(e.pointerId)` 與 `releasePointerCapture`，徹底杜絕快速滑動機甲時手指滑出畫布導致的「斷觸」與「掉針」。
     - 頂部 HUD 新增 `#fullscreenBtn` 全螢幕按鈕，點擊一鍵隱藏行動瀏覽器網址列。
     - CSS 安全區域 `env(safe-area-inset-top)` / `env(safe-area-inset-bottom)` 支援 iPhone 瀏海與動態島。
   - **區域網路直連伺服器腳本**：
     - `scripts/serve.js`：零外部相依 Node.js HTTP 伺服器，自動搜尋所有 Wi-Fi 網卡 IP（如 `http://192.168.x.x:8080`），並印出手機與平板連線指南。
     - `start-mobile-server.bat`：Windows 批次檔，雙擊即可一鍵啟動行動伺服器。

### 異動檔案
- `apps-script/Code.gs`：新增 `action: 'student_progress'` 端點、`getStudentProgressData_` 函數，完善 `registerStudent_` 寫入 Students 欄位對應。
- `manifest.json`：新增 PWA 描述檔。
- `starfall-quiz.html` & `index.html`：PWA 標頭、全螢幕按鈕、學員切換選單與錯題標籤組件。
- `style.css`：新增 `#fullscreenBtn`、safe-area-inset 邊界適配、`< 480px` 與 `< 640px` 行動裝置版面樣式微調。
- `game.js`：
  - `DataStore`：分學員隔離作答歷程、複合鍵 IndexedDB、雲端進度獨立同步、學員切換 UI 聯動。
  - `pickAdaptiveQuestions`：嚴格依指定學號篩選弱點題池與新題。
  - 指針事件：`setPointerCapture` 觸控防斷觸。
  - `fitStage()`：手機滿版、平板舒適比、桌面街機比三段式智慧縮放。
- `scripts/serve.js`：行動端區域網路伺服器。
- `start-mobile-server.bat`：一鍵啟動腳本。
- `scripts/test-v1.11.js`：BUILD-015 專屬自動化測試套件（40 項指標全部 PASS）。

### 測試方式
1. 執行 `node scripts/test-v1.11.js`：
   - PWA 與行動端檔案結構：全數 PASS。
   - 學員歷程 100% 獨立隔離模擬：學生 S0001 的錯題 Q001 不會流入 S0002；S0002 視 Q001 為新題目；S0001 復仇成功不影響 S0002，全數 PASS。
   - S0000 序號格式與 Students 試算表欄位對應：全數 PASS。
   - 多設備 fitStage 比例（iPhone 390x844 滿屏、iPad 舒適比、Desktop 9:16）：全數 PASS。
   - 指針防斷觸捕獲（setPointerCapture）：PASS。
2. 同步鏡像至 `C:\Users\何任軒\Desktop\Antigravity\starfall-quiz\`，於桌面環境執行測試亦 100% 通過。

---

## 2026-09-22 11:35 — BUILD-016：S0001 專屬權限隔離 (LAB / Google Sheet 設定) 與跨網域公網正式上線 (GitHub Pages)

### 完成項目
1. **S0001 (測試玩家) 專屬權限嚴格隔離**：
   - 僅有 `currentStudentId === 'S0001'`（測試玩家）能存取與看見以下管理工具：
     - 頂部 HUD 之「LAB」按鈕（`#labBtn`）。
     - 暫停選單（`#pauseModal`）底欄之「開啟 LAB」按鈕（`#pauseLabBtn`）。
     - 暫停選單（`#pauseModal`）底欄之「Google Sheet 設定」按鈕（`#pauseGsBtn`）。
     - 開始畫面中之「連線 Google Sheet」按鈕（`#openGsBtn`）。
   - 一般學員帳號（S0002、S0003...）或新學員註冊模式下，上述 4 大入口全面隱藏（`display: none`）。
   - 暫停選單底部按鈕列在一般學員狀態下，自動平順延伸為「繼續戰鬥」與「重新開始」兩大對稱按鈕，版面俐落乾淨。
   - 主動安全守衛：
     - `openLab()`：方法起始處檢查 `currentStudentId === 'S0001'`，若不符合則彈出 Toast 提示「權限不足：僅有 S0001 測試玩家可使用 LAB 面板」並拒絕開啟。
     - `openGs()`：檢查 `currentStudentId === 'S0001'`，若不符合則彈出提示並拒絕開啟。
   - 動態即時更新：新增 `updatePermissionUI()` 方法，於初始化、學員切換 (`switchStudent`)、下拉選單變更、新學員註冊與暫停呼叫時自動更新介面可見性。
2. **跨網域/外網公網遊玩正式上線 (GitHub Pages 部署)**：
   - 將專案初始化為 Git 倉庫，建立首個乾淨發行版本 Commit。
   - 透過 GitHub CLI (`gh`) 登入之 `r98921080` 帳號建立公開遠端倉庫 `https://github.com/r98921080/starfall-quiz` 並推送 `main` 分支。
   - 成功透過 GitHub API 開通 GitHub Pages 靜態網站代管服務：
     👉 **正式公網遊玩網址：`https://r98921080.github.io/starfall-quiz/`**
   - 學生、家長與教師在不在同一個網域（不同 Wi-Fi、4G/5G 行動網路、在家或不同校區）的情況下，均可直接透過該 HTTPS 網址在手機、平板與電腦上秒開暢玩。
   - 題庫獲取與作答歷程完全透過 Google Apps Script 雲端端點即時傳輸，全域不受內網或防火牆限制。
   - 新增 `deploy-to-web.bat` 一鍵部署更新批次腳本，未來任何程式碼修改雙擊即可自動推送更新。

### 異動檔案
- `game.js`：新增 `updatePermissionUI()`、於 5 大生命週期掛載、增設 `openLab()` 與 `openGs()` 權限守衛。
- `starfall-quiz.html` & `index.html`：保持完全同步。
- `style.css`：`.pause-footer` 彈性排版支援 2 鍵與 4 鍵自適應。
- `deploy-to-web.bat`：新增一鍵部署與更新 GitHub Pages 批次檔。
- `.gitignore`：排除系統暫存檔案。
- `scripts/test-v1.12.js`：新建 BUILD-016 專用測試套件（31 項指標全數 PASS）。

### 測試方式
1. 執行 `node scripts/test-v1.12.js`：
   - S0001 狀態：HUD LAB、暫停 LAB、暫停 Sheet、開始 Sheet 均顯示；openLab() 與 openGs() 成功放行。
   - S0002 狀態：上述 4 個按鈕全面隱藏（display: none）；openLab() 與 openGs() 守衛成功攔截。
   - 註冊新學員狀態：按鈕維持隱藏。
   - 跨網域設定：無寫死 localhost 或私有 IP，Apps Script 為公網 HTTPS。
   - 31 項測試全數 PASS。
2. 執行 `node scripts/test-v1.11.js`：40 項多設備與學生隔離測試全數 PASS。
3. 透過 `gh api /repos/r98921080/starfall-quiz/pages` 確認 GitHub Pages 服務已成功開通。

---

## 2026-09-22 12:15 — BUILD-017：公網遠端 Google Sheet 同步修復 (雙軌道 GET/POST)、多段武器防卡頓重構、16 款武器傷害評級 (S/A/B/C) 與開場首發第一階主武器鎖定

### 完成項目
1. **公網遠端 Google Sheet 同步修復 (雙軌道 GET / POST 雙重容錯備援)**：
   - **問題根因**：
     - 現行線上部署之 Google Apps Script Web App (`/exec`) 係基於舊版快照，尚未支援 `register_student` 操作，導致遠端登入時回傳 `{"ok":false,"error":"不支援的 action：register_student"}`。
     - 部分行動設備或特殊校園網路環境會阻斷帶有 body 的非標準 POST 請求或 CORS 重定向。
   - **解決方案**：
     - `apps-script/Code.gs`：
       - 全面升級 `doGet` 與 `doPost`，保證 `register_student`、`attempt`、`attempt_batch` 在 GET 與 POST 兩大管道均可 100% 成功結算。
       - 新增 `ensureStudentExists_` 機制：凡是有新學號或新姓名出現，即便未事前呼叫註冊，亦在作答寫入時自動於 `Students` 分頁登錄學生資料，杜絕孤兒紀錄。
     - `game.js`：
       - `syncStudentProfile` 與 `syncOfflineQueue` 實裝雙軌道傳輸（Dual-Channel Network Fallback）：先以 POST 發送，若遭遇網路異常或回應非 200，立刻切換至 GET 備援管道，確保公網學生資料不遺失。
2. **多段武器抗卡頓機制徹底重構 (Anti-Stutter Projectile Engine)**：
   - **卡頓機制根因**：
     - 穿透型武器（如金陽聚焦光束 `beam`、超空泡穿甲鏢 `kinetic_dart`、青玉飛輪 `jade_chakram`、玄天冰魄凌柱 `cryo_spire` 等）由於 pierce 高達 99，穿越雜兵或 Boss 時會在每幀（1/60 秒）持續觸發碰撞。
     - 原先碰撞邏輯每次命中單發大於 70 傷害即調用 `triggerHitStop(0.040)`，使引擎主迴圈硬生生停頓 40 毫秒（相當於直接掉 2.5 幀），多發穿透彈同時命中時 frame rate 暴跌至 10~20 FPS。
   - **重構解法**：
     - `Bullet` 實體建構子初始化 `this.hitEnemies = new Set()` 與 `this.hitMinions = new Set()`：所有子彈對同一個雜兵或召喚物**每趟穿透僅計算 1 次命中傷害**，杜絕連環重複判定。
     - Boss 碰撞節流鎖定：增加 `now - b.lastHitBossTime < 0.18` 離散間隔檢查，即使高 pierce 子彈滯留於 Boss 判定框內，傷害結算頻率也嚴格控制在每秒最多 5 次，不再造成每秒 60 次撞擊風暴。
     - 移除常規小怪命中與 `damageBoss` 的 routine `hitStopTimer` 引擎阻斷，保留畫面震動、受擊閃爍與火花粒子回饋，遊戲全局維持 60 FPS 絲滑順暢。
3. **16 款神話武器明確傷害評級 (S / A / B / C 四大階梯)**：
   - 依威力輸出、彈道覆蓋與戰術定位劃分 4 大層級：
     - **S 級 (毀滅神話)**：`熾陽熔岩噴射核`、`玄天冰魄凌柱`、`超聲震盪重砲`
     - **A 級 (強襲主力)**：`金陽聚焦光束`、`超空泡穿甲鏢`、`烈陽核融導彈`、`青玉風雷飛輪`
     - **B 級 (戰術壓制)**：`多管神機砲`、`神鳥隨行僚機`、`虹光折射星核`、`陰陽太極法陣`
     - **C 級 (守護輔助)**：`靈能聚變核心`、`虛空重力奇點`、`量子偏折護盾`、`翡翠靈泉護陣`、`躍遷時空擴張`
   - 全介面統一顯示專屬動態漸層評級徽章（`.tier-badge`）：整備機庫、答題三選一升級卡片、暫停武器庫 Inspector、LAB 面板。
   - 三選一升級機率加權：S 級與 A 級等高傷害武器出現機率適度調降（加權因子 S: 0.55, A: 0.85, B: 1.15, C: 1.40），符合 Roguelike 抽卡平衡。
4. **開場首發武器鎖定為第一階主武器 (Rank 1 Active Main Weapons Only)**：
   - 整備機庫 (`renderHangarWeaponsList`) 嚴格過濾排除所有被動支援模組（`isPassive: false`），僅開放 8 款主動主武器供玩者首發選擇。
   - 鎖定首發等級為第 1 階（Rank 1），被動裝備模組與更高階升級需透過戰鬥波次問答解鎖。
   - 更新首發狀態標籤：`當前首發主武：[武器名] (第 1 階) [Tier 級]`。

### 異動檔案
- `apps-script/Code.gs`：`doGet` 支援 `register_student`、`attempt`、`attempt_batch`；`normalizeAttempt_` 支援 `ensureStudentExists_`。
- `game.js`：
  - `STARFALL_WEAPONS_CATALOG` 配置 `tier` 與 `tierName`。
  - `Bullet` 構造子新增 `hitEnemies`、`hitMinions`、`lastHitBossTime`。
  - 碰撞迴圈防卡頓優化，移除常規 `triggerHitStop`。
  - `renderHangarWeaponsList` 嚴格過濾主動第 1 階主武。
  - `showUpgradeScreen` 與三選一卡片整合 tier badge 與出現權重調校。
  - `syncStudentProfile` 與 `syncOfflineQueue` 雙軌道 POST+GET 備援傳輸。
- `style.css`：新增 `.tier-badge`、`.tier-s`、`.tier-a`、`.tier-b`、`.tier-c` 視覺樣式。
- `index.html`：開場首發武裝提示文字優化。
- `scripts/test-v1.13.js`：BUILD-017 完整自動化驗證腳本。

### 測試方式
1. 執行 `node scripts/test-v1.13.js`：Code.gs 雙軌道、彈道防卡頓 Set 鎖、16 款武器階級、開場主武鎖定、CSS 樣式共 7 大項目全數 PASS。
2. 執行 `node scripts/test-v1.12.js`：31 項權限隔離與 GitHub Pages 測試全數 PASS。
3. 執行 `node scripts/test-v1.11.js`：40 項學生歷程隔離與多設備測試全數 PASS。

---

## 2026-09-22 12:20 — BUILD-018：開場起始武器嚴格限定 B 級或 C 級主動主武

### 完成項目
1. **起始武器選擇範圍調整 (限定 B 級與 C 級主動主武)**：
   - 根據使用者校正需求，開場不可直接起手 S 級（熾陽熔岩噴射核、超聲震盪重砲）或 A 級（金陽聚焦光束）神話武器，避免開局數值膨脹過度。
   - 調整 16 款武器評級配置：
     - **S 級 (3 款)**：`熾陽熔岩噴射核` (主動), `玄天冰魄凌柱` (被動), `超聲震盪重砲` (主動)
     - **A 級 (2 款)**：`金陽聚焦光束` (主動), `虹光折射星核` (被動)
     - **B 級 (5 款)**：`超空泡穿甲鏢` (主動), `烈陽核融導彈` (主動), `青玉風雷飛輪` (主動), `神鳥隨行僚機` (被動), `陰陽太極法陣` (主動)
     - **C 級 (6 款)**：`多管神機砲` (主動・基礎主砲), `靈能聚變核心` (被動), `虛空重力奇點` (被動), `量子偏折護盾` (被動), `翡翠靈泉護陣` (被動), `躍遷時空擴張` (被動)
   - 首發機庫清單 (`renderHangarWeaponsList`) 嚴格篩選條件：`!w.isPassive && (w.tier === 'B' || w.tier === 'C')`，開場可選擇武器共 5 款：
     1. 多管神機砲 (C 級)
     2. 超空泡穿甲鏢 (B 級)
     3. 烈陽核融導彈 (B 級)
     4. 青玉風雷飛輪 (B 級)
     5. 陰陽太極法陣 (B 級)
     （S 級、A 級與所有被動模組均被排除，需於答題戰鬥中晉升解鎖）。
2. **開局邏輯與安全防護 (`startNewGame`)**：
   - 增加 `validCandidates` 檢驗防線，若玩家所選武器非 B/C 級主動武器，自動回退預設 `multishot`（多管神機砲，第 1 階 C 級），防止任何非法起手。
3. **介面提示更新**：
   - 機庫按鈕：`⚙️ 初始武裝整備 (首發 B/C 級主武選擇)`。
   - 狀態標章：`當前首發主武：多管神機砲 (第 1 階) [C 級]`。
   - 機庫說明：`請挑選 1 款作為戰機開場出擊的第 1 階主武器（限定 B 級或 C 級主動主武；A 級、S 級與被動模組需於答題戰鬥中解鎖升級）：`。
4. **自動化測試套件更新 (`scripts/test-v1.13.js`)**：
   - 驗證起始候選主武恰好 5 款（1 款 C 級 + 4 款 B 級），且 100% 無 S 級、A 級或被動模組。
   - 驗證所有測試指標 100% 通過。

### 異動檔案
- `game.js`：更新武器評級，嚴格限定起始主武選擇為 B/C 級主動武器。
- `index.html`：更新首發 B/C 級武器說明與狀態徽章。
- `scripts/test-v1.13.js`：更新驗證斷言。
- `docs/BUILD-LOG.md`：記錄 BUILD-018 異動。

### 測試方式
1. 執行 `node scripts/test-v1.13.js`：7 大模組全數 PASS。
2. 執行 `node scripts/test-v1.12.js`：31 項測試全數 PASS。
3. 執行 `node scripts/test-v1.11.js`：40 項測試全數 PASS。


